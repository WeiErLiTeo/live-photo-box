v2.1.4 更新内容

新增：
- 💻 命令行新增 info 环境信息命令与 --version 快速版本查看
- ⏱️ merge 新增 --key-timestamp — 自定义封面在视频中的位置
- 🔄 CLI 新增手动更新模式 — update-check 检查后按安装方式提示更新，WinGet 安装引导 winget upgrade（WinGet 包待审核上架，上架后可用）

优化：
- 🎨 命令行彩色输出改版

💡 命令行模式仅随 GitHub 版发布，商店版暂不包含；需要请前往 GitHub Releases 下载。


v2.1.4 What's New

New:
- 💻 New `info` command and quick `--version` in the CLI
- ⏱️ merge gains `--key-timestamp` to set the key photo position
- 🔄 New manual update mode — `update-check` guides the update by install method; WinGet installs are directed to `winget upgrade` (package pending review, available once live)

Optimized:
- 🎨 Colorized CLI output

💡 CLI mode ships with the GitHub release only — the Store version does not include it. Download from GitHub Releases if you need it.
