<!-- PhotoClassifyPage.xaml — 照片分类页（占位，功能开发中） -->
<Page
    x:Class="LivePhotoBox.Views.PhotoClassifyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ic="using:FluentIcons.WinUI"
    mc:Ignorable="d"
    Background="Transparent"
    NavigationCacheMode="Required">

    <Grid Padding="24,24,24,0">
        <Border MaxWidth="720"
                Padding="32"
                CornerRadius="12"
                Background="{ThemeResource CustomPanelAcrylicBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel Spacing="12">
                <FontIcon Glyph="&#xED43;" FontSize="28" HorizontalAlignment="Center"/>
                <TextBlock x:Uid="PhotoClassifyPage_PlaceholderTitle"
                           FontSize="20"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Uid="PhotoClassifyPage_PlaceholderDescription"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
