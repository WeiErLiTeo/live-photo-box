<!-- 设置页（新版）：外观、转码、合成、拆分、修复配置及调试工具 -->
<Page
    x:Class="LivePhotoBox.Views.SettingsPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:ic="using:FluentIcons.WinUI"
    mc:Ignorable="d" Background="Transparent"
    NavigationCacheMode="Required">

    <!-- ═══════════════════════════════════════════════════════════════════════
         设置页面硬性规则（维护者必读）：

         1. 多语言
            页面中的所有文本（标题、描述、按钮、链接等）必须通过
            x:Uid 引用 Resources.resw，严禁在 Text / Content 属性中
            硬编码中文或任何语言的文字。

         2. 描述文本末尾不要句号
            所有 SettingsCard.Description 中的详细解释文本，
            末尾一律不加句号（。），保持简洁。
            例：✓ "用于查看 LivePhotoBox 处理过的照片操作记录"
                 ✗ "用于查看 LivePhotoBox 处理过的照片操作记录。"

         3. 调试工具区颜色
            本区域的交互控件（ToggleSwitch、Slider）必须使用红色
            （#D32F2F）覆写，不跟随主题色，参见下方注释。

         WinUI 3 注意事项见 docs/WinUI3开发注意事项.md
         ═══════════════════════════════════════════════════════════════════════ -->

    <!-- 布局：第0行固定标题，第1行可滚动内容 -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 固定标题 -->
        <TextBlock Grid.Row="0"
                   x:Uid="SettingsPage_Title" FontSize="32" FontWeight="Bold"
                   Margin="40,24,40,8" FontFamily="Microsoft YaHei UI"/>

        <ScrollViewer Grid.Row="1" x:Name="PageScrollViewer" Padding="40,0,40,0">
        <StackPanel x:Name="ContentPanel" Spacing="28" HorizontalAlignment="Stretch" Margin="0,0,0,40">

            <!-- ====================================================================== -->
            <!--  外观与个性化                                                         -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE790;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Appearance_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Language" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Language_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xF2B7;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{x:Bind ViewModel.LanguageIndex, Mode=TwoWay}"
                              Loaded="AppearanceComboBox_Loaded"
                              MinWidth="200">
                        <ComboBoxItem x:Uid="SettingsPage_Language_System"/>
                        <ComboBoxItem Content="English (en-US)"/>
                        <ComboBoxItem Content="中文(简体) (zh-Hans)"/>
                    </ComboBox>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
    <controls:SettingsCard.Header>
        <TextBlock x:Uid="SettingsPage_Theme_Header" FontFamily="Microsoft YaHei UI"/>
    </controls:SettingsCard.Header>
    <controls:SettingsCard.Description>
        <TextBlock x:Uid="SettingsPage_Theme_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
    </controls:SettingsCard.Description>
    <controls:SettingsCard.HeaderIcon>
        <FontIcon Glyph="&#xE790;" FontSize="16"/>
    </controls:SettingsCard.HeaderIcon>
    <ComboBox SelectedIndex="{x:Bind ViewModel.ElementTheme, Mode=TwoWay}"
              Loaded="AppearanceComboBox_Loaded"
              MinWidth="200">
        <ComboBoxItem x:Uid="SettingsPage_Theme_System"/>
        <ComboBoxItem x:Uid="SettingsPage_Theme_Light"/>
        <ComboBoxItem x:Uid="SettingsPage_Theme_Dark"/>
    </ComboBox>
</controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Backdrop_Header" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Backdrop_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEB91;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{x:Bind ViewModel.BackdropIndex, Mode=TwoWay}"
                              Loaded="AppearanceComboBox_Loaded"
                              MinWidth="200">
                        <ComboBoxItem x:Uid="SettingsPage_Backdrop_Mica"/>
                        <ComboBoxItem x:Uid="SettingsPage_Backdrop_MicaAlt"/>
                        <ComboBoxItem x:Uid="SettingsPage_Backdrop_Acrylic"/>
                        <ComboBoxItem x:Uid="SettingsPage_Backdrop_AcrylicLight"/>
                        <ComboBoxItem x:Uid="SettingsPage_Backdrop_None"/>
                    </ComboBox>
                </controls:SettingsCard>

                <!-- Acrylic 浓度滑块（仅选 Acrylic / Acrylic 薄透 时可见） -->
                <controls:SettingsCard FontFamily="Microsoft YaHei UI"
                                       Visibility="{x:Bind ViewModel.BackdropIndex, Mode=OneWay, Converter={StaticResource BackdropToAcrylicVisibilityConverter}}">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_AcrylicOpacity_Header" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_AcrylicOpacity_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="SquareHintSparkles" />
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Slider Minimum="0.0" Maximum="1.0"
                                Value="{x:Bind ViewModel.AcrylicTintOpacity, Mode=TwoWay}"
                                StepFrequency="0.05" Width="220" VerticalAlignment="Center"
                                ThumbToolTipValueConverter="{StaticResource DoubleToPercentConverter}"/>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Width="52" Height="36"
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                                       FontSize="13" FontWeight="SemiBold"
                                       Text="{x:Bind ViewModel.AcrylicTintOpacityText, Mode=OneWay}"/>
                        </Border>
                    </StackPanel>
                </controls:SettingsCard>

                <!-- 窗口整体透明度 -->
                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_WindowOpacity_Header" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_WindowOpacity_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xF5EF;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Slider Minimum="0.1" Maximum="1.0"
                                Value="{x:Bind ViewModel.WindowOpacity, Mode=TwoWay}"
                                StepFrequency="0.01" Width="220" VerticalAlignment="Center"
                                ThumbToolTipValueConverter="{StaticResource DoubleToPercentConverter}"/>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Width="52" Height="36"
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                                       FontSize="13" FontWeight="SemiBold"
                                       Text="{x:Bind ViewModel.WindowOpacityText, Mode=OneWay}"/>
                        </Border>
                    </StackPanel>
                </controls:SettingsCard>

                <!-- 主页背景（可折叠） -->
                <controls:SettingsExpander FontFamily="Microsoft YaHei UI">
                    <controls:SettingsExpander.Header>
                        <TextBlock x:Uid="SettingsPage_Banner_Header" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsExpander.Header>
                    <controls:SettingsExpander.Description>
                        <TextBlock x:Uid="SettingsPage_Banner_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsExpander.Description>
                    <controls:SettingsExpander.HeaderIcon>
                        <FontIcon Glyph="&#xF58A;" />
                    </controls:SettingsExpander.HeaderIcon>

                    <controls:SettingsExpander.Items>
                        <!-- 随机切换开关 -->
                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_BannerRandom_Header" FontFamily="Microsoft YaHei UI"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_BannerRandom_Desc" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE7F8;" FontSize="16"/>
                            </controls:SettingsCard.HeaderIcon>
                            <ToggleSwitch IsOn="{x:Bind ViewModel.IsBannerRandomEnabled, Mode=TwoWay}"
                                          x:Uid="Toggle_OnOff"/>
                        </controls:SettingsCard>

                        <!-- 折叠内容：左预览图 + 右当前预设名称 -->
                        <controls:SettingsCard ContentAlignment="Vertical"
                                               HorizontalContentAlignment="Stretch">
                            <controls:SettingsCard.Resources>
                                <x:Double x:Key="SettingsCardLeftIndention">0</x:Double>
                            </controls:SettingsCard.Resources>
                            <Grid Margin="0,8,0,8" HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 左侧：320×135 预览 + 切换箭头 -->
                                <Border Grid.Column="0" CornerRadius="8" Width="320" Height="135">
                                    <Grid>
                                        <Image Source="{x:Bind ViewModel.BannerImage0, Mode=OneWay}"
                                               Visibility="{x:Bind ViewModel.Banner0Visible, Mode=OneWay}"
                                               Stretch="UniformToFill"
                                               VerticalAlignment="Stretch"
                                               HorizontalAlignment="Center"/>
                                        <Image Source="{x:Bind ViewModel.BannerImage1, Mode=OneWay}"
                                               Visibility="{x:Bind ViewModel.Banner1Visible, Mode=OneWay}"
                                               Stretch="UniformToFill"
                                               VerticalAlignment="Stretch"
                                               HorizontalAlignment="Center"/>
                                        <Image Source="{x:Bind ViewModel.BannerImage2, Mode=OneWay}"
                                               Visibility="{x:Bind ViewModel.Banner2Visible, Mode=OneWay}"
                                               Stretch="UniformToFill"
                                               VerticalAlignment="Stretch"
                                               HorizontalAlignment="Center"/>
                                        <Button HorizontalAlignment="Left" VerticalAlignment="Center"
                                                Click="PrevBanner_Click"
                                                Background="#33000000" BorderThickness="0"
                                                Width="32" Height="48" Padding="0"
                                                CornerRadius="0,4,4,0">
                                            <FontIcon Glyph="&#xE76B;" FontSize="14" Foreground="White"/>
                                        </Button>
                                        <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                                                Click="NextBanner_Click"
                                                Background="#33000000" BorderThickness="0"
                                                Width="32" Height="48" Padding="0"
                                                CornerRadius="4,0,0,4">
                                            <FontIcon Glyph="&#xE76C;" FontSize="14" Foreground="White"/>
                                        </Button>
                                    </Grid>
                                </Border>

                                <!-- 右侧：当前预设名称 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                                    <TextBlock x:Uid="SettingsPage_Banner_CurrentLabel" FontSize="13"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.CurrentBannerPresetName, Mode=OneWay}"
                                               FontSize="18" FontWeight="SemiBold"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  通用                                                                 -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                    <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                        <FontIcon Glyph="&#xE713;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                        <TextBlock x:Uid="SettingsPage_General_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                    </StackPanel>

                    <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_General_RecursiveScan" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_General_RecursiveScan_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE838;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsRecursiveScanEnabled, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI"
                                       Visibility="{x:Bind ViewModel.OutputPreserveSubfolderVisibility, Mode=OneWay}">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_General_OutputPreserveSubfolder" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_General_OutputPreserveSubfolder_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE8A4;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsOutputPreserveSubfolderStructure, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>

                    <!-- 拖拽自动配对双文件实况照片 -->
                    <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                        <controls:SettingsCard.Header>
                            <TextBlock x:Uid="SettingsPage_General_DragDropAutoPair" FontFamily="Microsoft YaHei UI"/>
                        </controls:SettingsCard.Header>
                        <controls:SettingsCard.Description>
                            <TextBlock x:Uid="SettingsPage_General_DragDropAutoPair_Desc" FontSize="12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        </controls:SettingsCard.Description>
                        <controls:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE8F1;" FontSize="16"/>
                        </controls:SettingsCard.HeaderIcon>
                        <ToggleSwitch IsOn="{x:Bind ViewModel.IsDragDropAutoPairEnabled, Mode=TwoWay}"
                  x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard>

                    <!-- HEIC 缩略图解码方案（A/B 对比测试） -->
                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_General_ThumbnailProvider" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_General_ThumbnailProvider_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE8B9;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{x:Bind ViewModel.ThumbnailProviderIndex, Mode=TwoWay}"
                              Loaded="AppearanceComboBox_Loaded"
                              MinWidth="200">
                        <ComboBoxItem x:Uid="SettingsPage_General_ThumbnailProvider_MagickNET"/>
                        <ComboBoxItem x:Uid="SettingsPage_General_ThumbnailProvider_MagicScaler"/>
                    </ComboBox>
                </controls:SettingsCard>

            </StackPanel>

            <!-- ====================================================================== -->
            <!--  视频转码                                                             -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xEA69;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Transcode_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Transcode_Hardware" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Transcode_Hardware_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEEA1;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <ComboBox x:Name="HardwareComboBox"
                                  ItemsSource="{x:Bind ViewModel.AvailableHardware, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedHardware, Mode=TwoWay}"
                                  DisplayMemberPath="Name"
                                  Loaded="HardwareComboBox_Loaded"
                                  MinWidth="200"/>
                        <Button Width="32" Height="32" Padding="0"
                                x:Uid="SettingsPage_HardwareRefresh_Button"
                                Command="{x:Bind ViewModel.RefreshHardwareCommand, Mode=OneWay}">
                            <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                        </Button>
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Transcode_ThreadCount" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Transcode_ThreadCount_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="FastForward" />
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Slider Minimum="1"
                                Maximum="{x:Bind ViewModel.MaxThreadCount, Mode=OneWay}"
                                Value="{x:Bind ViewModel.ThreadCount, Mode=TwoWay}"
                                StepFrequency="1"
                                Width="220"
                                VerticalAlignment="Center"/>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="4"
                                MinWidth="36" Height="36"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind ViewModel.ThreadCount, Mode=OneWay}"
                                       TextAlignment="Center"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Margin="8,0"/>
                        </Border>
                    </StackPanel>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  实况照片编辑                                                            -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE8B2;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_LivePhotoEdit_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_TimelineMode_Header" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_TimelineMode_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE8B2;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{x:Bind ViewModel.TimelineModeIndex, Mode=TwoWay}"
                              MinWidth="200"
                              SelectionChanged="TimelineMode_SelectionChanged">
                        <ComboBoxItem x:Uid="SettingsPage_TimelineMode_Classic"/>
                        <ComboBoxItem x:Uid="SettingsPage_TimelineMode_Filmstrip"/>
                    </ComboBox>
                </controls:SettingsCard>

                <!-- 位置查询（反向地理编码） -->
                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_GeoLocation_Header" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_GeoLocation_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="Location" />
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsGeoLocationEnabled, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  实况照片合成                                                         -->
            <!-- ====================================================================== -->
            <StackPanel x:Name="MergeSettingsHeader" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE8B1;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Merge_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <StackPanel Spacing="2">
                            <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            <!-- 两个链接放在 WrapPanel 中，一行放不下时自动换行 -->
                            <toolkit:WrapPanel HorizontalSpacing="16" VerticalSpacing="4">
                                <HyperlinkButton FontSize="12"
                                                 Padding="0"
                                                 Click="OpenHeifStoreLink_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder_HeifLinkText" VerticalAlignment="Center"/>
                                        <FontIcon Glyph="&#xE8A7;" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </HyperlinkButton>
                                <HyperlinkButton FontSize="12"
                                                 Padding="0"
                                                 Click="OpenHevcStoreLink_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder_HevcLinkText" VerticalAlignment="Center"/>
                                        <FontIcon Glyph="&#xE8A7;" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </HyperlinkButton>
                            </toolkit:WrapPanel>
                        </StackPanel>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="DeveloperBoardSearch" />
                    </controls:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{x:Bind ViewModel.HeicDecoderIndex, Mode=TwoWay}"
                              Loaded="AppearanceComboBox_Loaded"
                              MinWidth="200">
                        <ComboBoxItem x:Uid="SettingsPage_Merge_HeicDecoder_MagickNET"/>
                        <ComboBoxItem x:Uid="SettingsPage_Merge_HeicDecoder_BitmapDecoder"/>
                    </ComboBox>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_ThreadCount" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Merge_ThreadCount_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="FastForward" />
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Slider Minimum="1"
                                Maximum="{x:Bind ViewModel.MaxMergeThreadCount, Mode=OneWay}"
                                Value="{x:Bind ViewModel.MergeThreadCount, Mode=TwoWay}"
                                StepFrequency="1"
                                Width="220"
                                VerticalAlignment="Center"/>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="4"
                                MinWidth="36" Height="36"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind ViewModel.MergeThreadCount, Mode=OneWay}"
                                       TextAlignment="Center"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Margin="8,0"/>
                        </Border>
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_GoogleForceMp4" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Merge_GoogleForceMp4_Desc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="VideoSync" />
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsGoogleProtocolForceMp4, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ═══════════════════════════════════════════════════════════════════════
                 实况照片拆分 — 已移至 SplitPage 页面底部栏
                 原设置项（视频输出格式）只有一个选项，没必要放在设置里，
                 且该选项很常用，直接放在拆分页面更方便。
                 保留以下代码以备将来可能恢复。
                 ═══════════════════════════════════════════════════════════════════════ -->
            <!--
            <StackPanel x:Name="SplitSettingsHeader" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE8C6;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Split_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Split_FormatLabel" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Split_FormatDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="VideoMultiple" />
                    </controls:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{x:Bind ViewModel.SplitFormatIndex, Mode=TwoWay}"
                              Loaded="AppearanceComboBox_Loaded"
                              MinWidth="200">
                        <ComboBoxItem x:Uid="SettingsPage_SplitFormat_ImageDefault"/>
                        <ComboBoxItem x:Uid="SettingsPage_SplitFormat_MP4"/>
                        <ComboBoxItem x:Uid="SettingsPage_SplitFormat_MOV"/>
                    </ComboBox>
                </controls:SettingsCard>
            </StackPanel>
            -->

            

            <!-- ====================================================================== -->
            <!--  实况照片显示修复                                                           -->
            <!-- ====================================================================== -->
            <StackPanel x:Name="RepairSettingsHeader" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE7C5;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Repair_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 仅修复 Apple 设备拍摄的照片 -->
                <controls:SettingsCard x:Name="AppleOnlyScanCard" FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_AppleOnlyScanLabel" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_AppleOnlyScanDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE90F;" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsAppleOnlyScanEnabled, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Repair_HeicLabel" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Repair_HeicDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="ScanCamera" FontSize="16"/>
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsHeicRepairEnabled, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Repair_LivePhotoVideoLabel" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Repair_LivePhotoVideoDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="VideoSettings" />
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsNonLivePhotoVideoRepairEnabled, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>

                <!-- 更严格的实况照片扫描 -->
                <Grid x:Name="StrictLivePhotoScanRoot">
                    <controls:SettingsCard x:Name="StrictLivePhotoScanCard" FontFamily="Microsoft YaHei UI">
                        <controls:SettingsCard.Header>
                            <TextBlock x:Uid="SettingsPage_StrictLivePhotoScanLabel" FontFamily="Microsoft YaHei UI"/>
                        </controls:SettingsCard.Header>
                        <controls:SettingsCard.Description>
                            <TextBlock x:Uid="SettingsPage_StrictLivePhotoScanDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        </controls:SettingsCard.Description>
                        <controls:SettingsCard.HeaderIcon>
                            <ic:SymbolIcon Symbol="FolderSearch" />
                        </controls:SettingsCard.HeaderIcon>
                        <ToggleSwitch IsOn="{x:Bind ViewModel.IsStrictLivePhotoScanEnabled, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard>
                    <Border x:Name="StrictLivePhotoScanHighlight" IsHitTestVisible="False"
                            Background="Transparent" CornerRadius="4"/>
                </Grid>

                <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Repair_CopyPerfectLabel" FontFamily="Microsoft YaHei UI"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Repair_CopyPerfectDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.HeaderIcon>
                        <ic:SymbolIcon Symbol="ImageArrowForward" />
                    </controls:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsCopyPerfectToOutput, Mode=TwoWay}"
                                  x:Uid="Toggle_OnOff"/>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  调试工具区                                                           -->
            <!-- ====================================================================== -->
            <StackPanel Margin="0,0,0,0" Spacing="16">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton Grid.Column="0"
                                  IsChecked="{x:Bind IsTestToolsVisible, Mode=TwoWay}"
                                  HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xEC7A;" FontSize="14"/>
                            <TextBlock Text="{x:Bind TestToolsToggleButtonText, Mode=OneWay}" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ToggleButton>

                    <Button Grid.Column="1"
                            Click="RestoreDefaultSettings_Click"
                            HorizontalAlignment="Right"
                            BorderThickness="0"
                            Style="{ThemeResource DefaultButtonStyle}">
                        <Button.Resources>
                            <SolidColorBrush x:Key="ButtonBackground" Color="#D32F2F"/>
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#EF5350"/>
                            <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#C62828"/>
                            <SolidColorBrush x:Key="ButtonForeground" Color="White"/>
                            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White"/>
                            <SolidColorBrush x:Key="ButtonForegroundPressed" Color="White"/>
                            <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent"/>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE72C;" FontSize="14" Foreground="White"/>
                            <TextBlock x:Uid="SettingsPage_Restore_Button_Text" FontSize="13" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <TextBlock Text="{x:Bind CrashNoticeText, Mode=OneTime}"
                           Visibility="{x:Bind CrashNoticeVisibility, Mode=OneWay}"
                           Foreground="{ThemeResource SystemErrorTextColor}"
                           FontSize="12"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,16"/>

                <StackPanel Spacing="20"
                            Visibility="{x:Bind TestToolsVisibility, Mode=OneWay}">

                    <!-- 应用日志 -->
                    <StackPanel Spacing="4">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                            <FontIcon Glyph="&#xE7BA;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                            <TextBlock x:Uid="SettingsPage_Crash_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                        </StackPanel>

                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.HeaderIcon>
                                <ic:SymbolIcon Symbol="ClipboardTextEdit" />
                            </controls:SettingsCard.HeaderIcon>
                            <controls:SettingsCard.Description>
                                <StackPanel Spacing="12">
                                    <!-- 本次日志 + 上一次日志（同一排，中间留空） -->
                                    <StackPanel Orientation="Horizontal" Spacing="24" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                            <TextBlock x:Uid="SettingsPage_CrashCurrentTime_Label" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                            <HyperlinkButton Command="{x:Bind ViewModel.OpenCurrentLogActionCommand, Mode=OneWay}" Padding="0" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind ViewModel.CurrentLogFileNameText, Mode=OneWay}" TextWrapping="Wrap" FontSize="15" FontWeight="SemiBold"/>
                                            </HyperlinkButton>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                            <TextBlock x:Uid="SettingsPage_CrashLastTime_Label" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                            <HyperlinkButton Command="{x:Bind ViewModel.OpenPreviousLogActionCommand, Mode=OneWay}" Padding="0" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind ViewModel.PreviousLogFileNameText, Mode=OneWay}" TextWrapping="Wrap" FontSize="15" FontWeight="SemiBold"/>
                                            </HyperlinkButton>
                                        </StackPanel>
                                    </StackPanel>
                                    <toolkit:WrapPanel HorizontalSpacing="8" VerticalSpacing="8">
                                        <Button Command="{x:Bind ViewModel.OpenCrashLogFolderActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE838;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashOpenFolder_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind ViewModel.ExportCurrentLogActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashExportCurrent_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind ViewModel.ExportPreviousLogActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashExportLatest_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind ViewModel.ClearCrashLogsActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashClear_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind ViewModel.OpenIssueFeedbackActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE8A7;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashIssue_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </toolkit:WrapPanel>

                                    <TextBlock x:Uid="SettingsPage_CrashLogPrivacyHint"
                                               FontSize="11"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </controls:SettingsCard.Description>
                        </controls:SettingsCard>
                    </StackPanel>

                    <!-- ==================== 调试工具 — 全部统一使用 SettingsCard ==================== -->


                    <!-- ⚠️ 调试工具区通用规范 ⚠️

                         颜色：
                         本区域所有能改变主题色的交互控件（ToggleSwitch、Slider、按钮等），
                         必须统一使用红色（#D32F2F 及其 Hover/Pressed 变体）作为覆写颜色，
                         不得跟随系统主题色（SystemAccentColor）。
                         原因：调试工具属于危险/实验性功能，红色起到"警告"作用，
                         提醒用户这些设置可能会影响程序正常运行。

                         开关文本：
                         本区域的所有 ToggleSwitch 必须使用 x:Uid="Toggle_EnabledDisabled"，
                         使其显示"已启用 / 已禁用"而不是默认的"开 / 关"，
                         语义更符合调试功能的性质。

                         如果新增任何带颜色的控件（开关、滑块、按钮、单选等），
                         请参照下方已有控件的 Resources 写法添加红色覆写。 -->
                    <StackPanel Spacing="4">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                            <FontIcon Glyph="&#xEBE8;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                            <TextBlock x:Uid="SettingsPage_Test_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                        </StackPanel>

                        <TextBlock x:Uid="SettingsPage_Test_Warning"
                                   FontSize="12"
                                   Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12"/>



                        <!-- 操作按钮 -->
                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xEC7A;" FontSize="16"/>
                            </controls:SettingsCard.HeaderIcon>
                            <controls:SettingsCard.Description>
                                <toolkit:WrapPanel HorizontalSpacing="8" VerticalSpacing="8">
                                    <Button Click="CheckUpdate_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE895;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_CheckUpdate_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Click="RestartAppButton_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_RestartApp_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Click="CheckExternalTools_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE9D2;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_CheckTools_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </toolkit:WrapPanel>
                            </controls:SettingsCard.Description>
                        </controls:SettingsCard>

                        <!-- GitHub API Token（仅非打包模式可见，绕过未认证 60次/小时限流） -->
                        <controls:SettingsCard x:Name="GitHubTokenCard" FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_Debug_GitHubTokenHeader" FontFamily="Microsoft YaHei UI"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <StackPanel Spacing="2">
                                    <TextBlock x:Uid="SettingsPage_Debug_GitHubTokenDesc" FontSize="12"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                                    <HyperlinkButton FontSize="12"
                                                     Padding="0"
                                                     Click="OpenGitHubTokenGuide_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <TextBlock x:Uid="SettingsPage_Debug_GitHubTokenGuideLink" VerticalAlignment="Center"/>
                                            <FontIcon Glyph="&#xE8A7;" FontSize="11" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </HyperlinkButton>
                                </StackPanel>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE72E;" FontSize="16"/>
                            </controls:SettingsCard.HeaderIcon>
                            <StackPanel Orientation="Vertical" Spacing="8" MinWidth="320" Margin="0,4,0,4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Grid Width="280" Height="32" VerticalAlignment="Center">
                                        <PasswordBox x:Name="GitHubTokenBox"
                                                     Width="280" Height="32"
                                                     IsPasswordRevealButtonEnabled="False"
                                                     VerticalAlignment="Center"/>
                                        <Button x:Name="ClearTokenBoxBtn"
                                                Width="28" Height="28"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Margin="0,0,2,0"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="0"
                                                Click="ClearTokenBox_Click">
                                            <FontIcon Glyph="&#xE711;" FontSize="10"
                                                      Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                        </Button>
                                    </Grid>
                                    <Button x:Name="SaveGitHubTokenBtn"
                                            x:Uid="SettingsPage_Debug_GitHubTokenSaveBtn"
                                            Click="SaveGitHubToken_Click"
                                            Height="32" Padding="12,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                <Grid>
                                    <TextBlock x:Name="GitHubTokenStatus"
                                               FontSize="11"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               TextWrapping="Wrap"/>
                                    <Button x:Name="ClearGitHubTokenBtn"
                                            x:Uid="SettingsPage_Debug_GitHubTokenClearBtn"
                                            Click="ClearGitHubToken_Click"
                                            HorizontalAlignment="Right"
                                            Height="24" Padding="8,0"
                                            FontSize="11"
                                            Visibility="Collapsed"/>
                                </Grid>
                            </StackPanel>
                        </controls:SettingsCard>

                        <!-- 照片历史页面显示开关 -->
                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_Debug_PhotoHistoryHeader" FontFamily="Microsoft YaHei UI"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_Debug_PhotoHistoryDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE81C;" FontSize="16"/>
                            </controls:SettingsCard.HeaderIcon>
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <Button Click="OpenHistoryPage_Click" MinWidth="0" Padding="8,4"
                                        Visibility="{x:Bind ViewModel.IsHistoryPageVisible, Converter={StaticResource InverseVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                        <FontIcon Glyph="&#xE8E5;" FontSize="12"/>
                                        <TextBlock x:Uid="SettingsPage_Debug_OpenHistoryBtn" FontSize="12"/>
                                    </StackPanel>
                                </Button>
                                <ToggleSwitch IsOn="{x:Bind ViewModel.IsHistoryPageVisible, Mode=TwoWay}"
                                              x:Uid="Toggle_EnabledDisabled">
                                    <!-- 警告：调试工具区的开关统一使用红色（警告色），文本显示"已启用/已禁用"，不要跟随主题色 -->
                                    <ToggleSwitch.Resources>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="#D32F2F"/>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="#EF5350"/>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="#C62828"/>
                                    </ToggleSwitch.Resources>
                                </ToggleSwitch>
                            </StackPanel>
                        </controls:SettingsCard>

                        <!-- 写入照片操作记录（红色 = 调试工具区警告色，不要跟随主题色）
                             打开后会记录本软件对照片所做的合成、拆分、修复等操作历史，
                             可在照片历史页面中查看完整的处理记录。 -->
                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_Debug_WriteHistoryHeader" FontFamily="Microsoft YaHei UI"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_Debug_WriteHistoryDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.HeaderIcon>
                                <ic:SymbolIcon Symbol="DocumentImage" />
                            </controls:SettingsCard.HeaderIcon>
                            <ToggleSwitch IsOn="{x:Bind ViewModel.IsDetailedHistoryEnabled, Mode=TwoWay}"
                                          x:Uid="Toggle_EnabledDisabled">
                                <ToggleSwitch.Resources>
                                    <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="#D32F2F"/>
                                    <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="#EF5350"/>
                                    <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="#C62828"/>
                                </ToggleSwitch.Resources>
                            </ToggleSwitch>
                        </controls:SettingsCard>

                        <!-- 扫描缩略图开关（红色 = 调试工具区警告色，不要跟随主题色） -->
                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_ScanThumbnailToggleLabel" FontFamily="Microsoft YaHei UI"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_ScanThumbnailToggleDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.HeaderIcon>
                                <ic:SymbolIcon Symbol="FolderSearch" />
                            </controls:SettingsCard.HeaderIcon>
                            <ToggleSwitch IsOn="{x:Bind ViewModel.IsRepairScanLoadThumbnail, Mode=TwoWay}"
                                          x:Uid="Toggle_EnabledDisabled">
                                <ToggleSwitch.Resources>
                                    <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="#D32F2F"/>
                                    <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="#EF5350"/>
                                    <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="#C62828"/>
                                </ToggleSwitch.Resources>
                            </ToggleSwitch>
                        </controls:SettingsCard>

                        <!-- HEIC 预览并发数（红色 = 调试工具区警告色，不要跟随主题色） -->
                        <controls:SettingsCard FontFamily="Microsoft YaHei UI">
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_HeicConcurrencyLabel" FontFamily="Microsoft YaHei UI"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_HeicConcurrencyDesc" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE7C5;" FontSize="16"/>
                            </controls:SettingsCard.HeaderIcon>
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <Slider Minimum="1"
                                        Maximum="{x:Bind ViewModel.MaxHeicConcurrency, Mode=OneWay}"
                                        Value="{x:Bind ViewModel.HeicConcurrency, Mode=TwoWay}"
                                        StepFrequency="1"
                                        Width="220"
                                        VerticalAlignment="Center">
                                    <Slider.Resources>
                                        <SolidColorBrush x:Key="SliderTrackValueFill" Color="#D32F2F"/>
                                        <SolidColorBrush x:Key="SliderTrackValueFillPointerOver" Color="#EF5350"/>
                                        <SolidColorBrush x:Key="SliderTrackValueFillPressed" Color="#C62828"/>
                                        <SolidColorBrush x:Key="SliderThumbBackground" Color="#D32F2F"/>
                                        <SolidColorBrush x:Key="SliderThumbBackgroundPointerOver" Color="#EF5350"/>
                                        <SolidColorBrush x:Key="SliderThumbBackgroundPressed" Color="#C62828"/>
                                    </Slider.Resources>
                                </Slider>
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1" CornerRadius="4"
                                        MinWidth="36" Height="36"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind ViewModel.HeicConcurrency, Mode=OneWay}"
                                               TextAlignment="Center"
                                               FontSize="15"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               Margin="8,0"/>
                                </Border>
                            </StackPanel>
                        </controls:SettingsCard>

                    </StackPanel>
                </StackPanel>

                <TextBlock x:Uid="SettingsPage_Footer"
                           FontSize="12" Opacity="0.3"
                           HorizontalAlignment="Center"
                           Margin="0,38,0,20"/>
            </StackPanel>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
