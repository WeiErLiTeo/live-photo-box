<!-- App.xaml — 全局资源字典：值转换器、主题画笔、按钮样式、浅/深色主题色 -->
<Application
    x:Class="LivePhotoBox.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:LivePhotoBox.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- 值转换器 -->
            <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
            <converters:ProgressBarForegroundConverter x:Key="ProgressBarForegroundConverter"/>
            <converters:ProgressBarIndeterminateConverter x:Key="ProgressBarIndeterminateConverter"/>
            <converters:BoolToDiagnosisErrorBrushConverter x:Key="BoolToDiagnosisErrorBrushConverter"/>
            <converters:BackdropToAcrylicVisibilityConverter x:Key="BackdropToAcrylicVisibilityConverter"/>
            <converters:DoubleToPercentConverter x:Key="DoubleToPercentConverter"/>
            <converters:VisibilityConverter x:Key="VisibilityConverter"/>
            <converters:InverseVisibilityConverter x:Key="InverseVisibilityConverter"/>
            <converters:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
            <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>

            <!-- 主题色 -->
            <SolidColorBrush x:Key="CyberCyanBrush" Color="#00E5FF"/>
            <SolidColorBrush x:Key="CyberPurpleBrush" Color="#B388FF"/>
            <!-- 卡片面板底色：使用系统卡片背景色，随浅色/深色主题自动切换。
                 导航区内容已透明 → 窗口背景材质（Mica/Acrylic）从卡片间隙透出。 -->
            <SolidColorBrush x:Key="CustomPanelAcrylicBrush" Color="{ThemeResource CardBackgroundFillColorDefault}" />

            <!-- 取消扫描按钮配色 -->
            <SolidColorBrush x:Key="ScanCancelButtonBackgroundBrush" Color="#C42B1C"/>
            <SolidColorBrush x:Key="ScanCancelButtonBackgroundPointerOverBrush" Color="#A82418"/>
            <SolidColorBrush x:Key="ScanCancelButtonBackgroundPressedBrush" Color="#8B1E14"/>
            <SolidColorBrush x:Key="ScanCancelButtonForegroundBrush" Color="#FFFFFF"/>

            <!-- 取消扫描按钮样式（红色警示风格） -->
            <Style x:Key="ScanCancelButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource ScanCancelButtonBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource ScanCancelButtonForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ScanCancelButtonBackgroundBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}"/>
                <Setter Property="Padding" Value="{ThemeResource ButtonPadding}"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="UseSystemFocusVisuals" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid
                                x:Name="RootGrid"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ScanCancelButtonBackgroundPointerOverBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ScanCancelButtonBackgroundPointerOverBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ScanCancelButtonBackgroundPressedBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ScanCancelButtonBackgroundPressedBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" To="0.55" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="Center"
                                    VerticalContentAlignment="Center"
                                    AutomationProperties.AccessibilityView="Raw"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Foreground="{TemplateBinding Foreground}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 浅/深/默认 主题色字典 -->
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <!-- Card 背景改为与 SettingsCard 同款的系统画笔，统一材质感 -->
                    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}"/>
                    <SolidColorBrush x:Key="PageStatusBarBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}"/>
                    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#E5E5E5"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <!-- Card 背景改为与 SettingsCard 同款的系统画笔，统一材质感 -->
                    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}"/>
                    <SolidColorBrush x:Key="PageStatusBarBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}"/>
                    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#3D3D3D"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Default">
                    <!-- Card 背景改为与 SettingsCard 同款的系统画笔，统一材质感 -->
                    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}"/>
                    <SolidColorBrush x:Key="PageStatusBarBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}"/>
                    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#E5E5E5"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
