<div align="center">

# 实况照片协议分析报告

**本报告基于对真实设备文件的逆向分析编写**，覆盖各主流厂商的实况照片协议，分析其字节布局、XMP 元数据与识别逻辑。

</div>

> **创建日期**: 2026-07-20 · **项目**: [Live Photo Box](https://github.com/lengxiqwq/live-photo-box) · **作者**: [LengxiQwQ](https://github.com/lengxiqwq) · **反馈**: [GitHub Issues](https://github.com/lengxiqwq/live-photo-box/issues)

---

> **图例**：🔴 = 待确认 &nbsp;|&nbsp; 🟡 = 推测


## 📖 基本概念

实况照片（Live Photo / Motion Photo / Micro Video）由一张静态图片与一段短视频组成。拍摄时设备记录按下快门前后约 1.5~3 秒的画面，浏览时可播放动态效果。

各厂商的实现按**打包方式**分为两类：

| 类型 | 打包方式 | 厂商 |
|------|----------|------|
| **单文件** | 一个 `.jpg` 文件：JPEG 图片数据在前，MP4 视频数据拼接在后，视频位置由 XMP 元数据中的偏移量/长度字段指定 | Google、OPPO、小米、三星、vivo（X300 系列起，3-item Ultra HDR） |
| **单文件（私有）** | 一个 `.heic` 或 `.jpg` 文件，不使用 XMP 标准，通过私有二进制标记定位视频 | 华为 Moving |
| **双文件** | 一个图片文件 + 一个视频文件，两者通过元数据中的 `ContentIdentifier` UUID 配对 | Apple Live |
| **双文件（私有）** | 一个 `.jpg` + 一个 `.mp4`，通过 vivo 私有 ID 关联（旧格式，≤X200 系列） | vivo（≤X200 系列） |

单文件格式的核心机制：JPEG 的 XMP（Adobe Extensible Metadata Platform）元数据中记录了视频的长度（字节数），图片部分长度 = 文件总大小 − 视频长度。XMP 数据存储在 JPEG 的 APP1 段中。

双文件格式由于图片与视频本就是独立文件，不存在"定位视频"的问题；核心机制是**元数据配对**——照片和视频共享相同的 `ContentIdentifier` UUID。

---

## 📦 单文件格式

单文件格式的通用二进制布局：

```
┌──────────────────────┐  ← 文件起始 (offset 0)
│   JPEG 图片数据       │
│   (含 EXIF + XMP)    │
├──────────────────────┤  ← 图片结束位置
│   MP4 视频数据        │
└──────────────────────┘  ← 文件末尾

```
各协议的区别在于 XMP 元数据中使用的命名空间和字段不同。

---

### Google MicroVideo V1

Google 最早的实况照片格式，已弃用，但旧设备（旧 Pixel、MIUI 时代的小米）仍大量使用。


#### 二进制结构

与通用布局一致。JPEG 尾部直接拼接 MP4（ftyp=mp42/isom）。


#### XMP 元数据

使用 `GCamera` 命名空间，自闭合 `rdf:Description` 标签，共 4 个数据属性：

```xml
<rdf:Description rdf:about=""
    xmlns:GCamera="http://ns.google.com/photos/1.0/camera/"
    GCamera:MicroVideo="1"
    GCamera:MicroVideoVersion="1"
    GCamera:MicroVideoOffset="4424761"
    GCamera:MicroVideoPresentationTimestampUs="459334"/>

```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `GCamera:MicroVideo` | 整数 | 实况照片标识。值为 `1` |
| `GCamera:MicroVideoVersion` | 整数 | 协议版本，固定为 `1` |
| `GCamera:MicroVideoOffset` | 整数 | 视频字节数。从文件末尾向前推算：图片长度 = 文件总大小 − 此值 |
| `GCamera:MicroVideoPresentationTimestampUs` | 整数 | 封面帧在视频中的微秒时间戳。`0` = 视频开头 |


#### 提取逻辑

1. 读取 `MicroVideoOffset` 值
2. 图片部分 = 文件前 `totalSize − MicroVideoOffset` 字节
3. 视频部分 = 文件后 `MicroVideoOffset` 字节


#### 封面帧机制

`GCamera:MicroVideoPresentationTimestampUs` 直接给出封面帧在视频中的微秒位置。例如值为 `459334` 即视频第 0.46 秒处为封面。值为 `0` 表示封面在视频开头。

---

### Google MotionPhoto V2

Google 的现代实况照片标准。使用 `Container:Directory` 结构化 XMP 描述文件内容。目前兼容性最广，Windows 11 文件资源管理器可原生预览。

- 规范名称：**Motion Photo format 1.0**
- 官方文档：<https://developer.android.com/media/platform/motion-photo-format>

使用者：Google Pixel、三星 Galaxy、小米（HyperOS）、摩托罗拉等。


#### 二进制结构（JPEG）

JPEG 图片数据在前，视频数据（MP4 或 MOV）直接追加在 JPEG EOI（`FF D9`）之后，无分隔符。

规范要求：Container:Directory 中各 Item **必须**按其在文件中出现的实际顺序排列，且紧密排列（tightly packed）。`MotionPhoto` Item **必须**位于文件末尾，其后**不允许**有任何其他字节。


#### XMP 元数据

使用 `GCamera`、`Container`、`Item` 三个命名空间。注意：官方规范中 Camera 命名空间的**默认前缀**为 `Camera`，但各厂商实现普遍使用 `GCamera`——XMP 以 URI 为准，前缀可任意，二者等价。

```xml
<rdf:Description rdf:about=""
    xmlns:GCamera="http://ns.google.com/photos/1.0/camera/"
    xmlns:Container="http://ns.google.com/photos/1.0/container/"
    xmlns:Item="http://ns.google.com/photos/1.0/container/item/"
    GCamera:MotionPhoto="1"
    GCamera:MotionPhotoVersion="1"
    GCamera:MotionPhotoPresentationTimestampUs="1230275">
  <Container:Directory>
    <rdf:Seq>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Mime="image/jpeg"
                        Item:Semantic="Primary"
                        Item:Length="0"
                        Item:Padding="0"/>
      </rdf:li>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Mime="video/mp4"
                        Item:Semantic="MotionPhoto"
                        Item:Length="4424761"
                        Item:Padding="0"/>
      </rdf:li>
    </rdf:Seq>
  </Container:Directory>
</rdf:Description>

```
> MicroVideo V1 的四个字段（`MicroVideo`、`MicroVideoVersion`、`MicroVideoOffset`、`MicroVideoPresentationTimestampUs`）在本规范中**已删除**，解析时**必须忽略**。视频定位应使用 `Container:Directory` 中的 `Item:Length`，而非 V1 的 `MicroVideoOffset`。


#### 字段说明

**Camera 元数据**（命名空间 `http://ns.google.com/photos/1.0/camera/`）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `MotionPhoto` | `Integer` | `0` = 不是 Motion Photo；`1` = 是 Motion Photo。**其他值均视为未定义，等效于 `0`**。由于多数图片编辑器会保留 XMP，即使视频已被移除，该字段仍可能残留值为 `1`。因此该字段**不是决定性证据**——阅读器必须实际确认视频是否存在。 |
| `MotionPhotoVersion` | `Integer` | 协议版本。本规范定义为 `1`。 |
| `MotionPhotoPresentationTimestampUs` | `Long` | 封面帧在视频中的微秒时间戳。可为 `-1` 表示未设置。 |

**Container 元数据**（命名空间 `http://ns.google.com/photos/1.0/container/`）：

`Container:Directory` 是一个有序的 `rdf:Seq` 结构。**有且仅有一个** `Primary` Item，且必须排在第一位。

**Item 元数据**（命名空间 `http://ns.google.com/photos/1.0/container/item/`）：

| 属性 | 类型 | 说明 |
|------|------|------|
| `Mime` | `String` | **必填**。MIME 类型：`image/jpeg`、`image/heic`、`image/avif`、`video/mp4`、`video/quicktime` |
| `Semantic` | `String` | **必填**。`Primary`=主图片（有且仅有一个），`MotionPhoto`=实况视频（有且仅有一个），`GainMap`=Ultra HDR 增益图（可选） |
| `Length` | `Integer` | 次级 Item **必填**。资源字节长度。Primary Item 应为 `0`（图片长度由 MIME 类型解析确定）。视频的 `Length` 使阅读器可直接从文件末尾倒退 `<Length>` 字节定位视频起点。 |
| `Padding` | `Integer` | 仅 Primary Item 可有此属性。JPEG 格式下可选。表示主图片编码结束到下一媒体项之间的填充字节数。 |

> 实践观察：小米 14 Pro（HyperOS）的原厂样本中，Primary Item 缺少 `Item:Length` 与 `Item:Padding` 属性。Google 规范允许解析器通过 JPEG 自身确定主图片长度，因此实现时应对此情况做容错处理。


#### 封面帧机制

`MotionPhotoPresentationTimestampUs` 指定封面帧在视频中的微秒位置。例如 `1230275` → 视频第 1.230275 秒。

**该字段缺失时的默认行为**（官方规范明确定义）：阅读器应使用视频时长**除以 2**，选择最接近中点的时间戳前一帧作为封面——**不是**"自行选择"。


#### V2 相比 V1 的改进

| | MicroVideo V1 | MotionPhoto V2 |
|------|:---:|:---:|
| 资源描述 | 单一 `MicroVideoOffset` 字段 | `Container:Directory` 结构化目录 |
| MIME 类型 | 隐式 | 显式声明（`image/jpeg`、`video/mp4`、`video/quicktime` 等） |
| 可扩展性 | 不可扩展 | `rdf:Seq` 可添加更多资源项（如 Ultra HDR GainMap） |
| 填充支持 | 无 | `Item:Padding` 支持 |
| V1 兼容字段 | — | 规范要求忽略 V1 的四个字段 |
| 当前状态 | 已弃用 | 现行标准 |

---

### 小米 Mi-Live Photo

小米直接使用 Google 标准协议（早期 MIUI → MicroVideo V1；当前 HyperOS → MotionPhoto V2），**无独立协议**。小米相册完全依赖 V2 识别和播放实况照片，不限制读取自家元数据——符合 V2 标准的其他品牌（如 OPPO）实况照片均可播放。


#### 二进制结构

与 Google V2 一致——JPEG 图片在前，视频追加在文件末尾。较新机型可能包含 GainMap 增益图：

```
┌──────────────────────┐
│   JPEG 图片           │
│   (含 EXIF + XMP)     │
├──────────────────────┤
│   GainMap 增益图      │  ← Ultra HDR，非实况照片协议内容
├──────────────────────┤
│   MP4 视频            │
└──────────────────────┘

```

#### XMP 元数据

完全使用 Google MotionPhoto V2 标准，无私有扩展字段。`Container:Directory` 中除 Primary 和 MotionPhoto 外可能包含 GainMap Item（HDR 增益图，与实况无关）。

小米额外写入一个 `MiCamera` 命名空间（`http://ns.xiaomi.com/photos/1.0/camera/`），当前格式为 `MiCamera:XMPMeta`，内容为 XML 编码的 `video_madrid_image` 元数据。该字段 `offset="0" length="0"`，**不参与视频定位**——视频定位完全由 V2 的 `Item:Length` 决定。🔴 早期 HyperOS 版本是否使用 `MiCamera:VideoLength` 待确认。

```xml
<rdf:Description rdf:about=""
    xmlns:GCamera="http://ns.google.com/photos/1.0/camera/"
    xmlns:Container="http://ns.google.com/photos/1.0/container/"
    xmlns:Item="http://ns.google.com/photos/1.0/container/item/"
    xmlns:MiCamera="http://ns.xiaomi.com/photos/1.0/camera/"
    GCamera:MotionPhoto="1"
    GCamera:MotionPhotoVersion="1"
    GCamera:MotionPhotoPresentationTimestampUs="1533248"
    MiCamera:XMPMeta="&lt;video_madrid_image type=&quot;701&quot;
      offset=&quot;0&quot; length=&quot;0&quot;
      width=&quot;3072&quot; height=&quot;4096&quot;
      paddingx=&quot;86&quot; paddingy=&quot;86&quot;
      location_enabled=&quot;0&quot; time_enabled=&quot;0&quot; dark=&quot;0&quot; /&gt;">
  <Container:Directory>
    <rdf:Seq>
      <rdf:li>  <!-- Primary: image/jpeg -->
      <rdf:li>  <!-- GainMap: image/jpeg（HDR，与实况无关） -->
      <rdf:li>  <!-- MotionPhoto: video/mp4 -->
    </rdf:Seq>
  </Container:Directory>
</rdf:Description>

```

#### 私有 EXIF 标签

| Tag (Hex) | IFD | 内容 | 与实况相关？ |
|-----------|-----|------|:---:|
| `0x010f` Make | IFD0 | 固定为 `"Xiaomi"` | 否 |
| `0x0110` Model | IFD0 | 设备型号 | 否 |
| `0x9a00` XiaomiModel | IFD0 | 小米内部型号名 | 否 |
| `0x889e` | ExifIFD | JSON 设备信息（`buildDevice`、`livephotoInfo`、`waterLogo` 等），详见下方 | 否 |
| `0x8895` | ExifIFD | `0`（版本标记，🔴 "新=3"说法待验证） | 🔴 |
| `0x8889` | ExifIFD | `"HDR"` | 🔴 |
| `0x8897` | ExifIFD | `1` | 🔴 |
| `0x889a` | IFD0 + ExifIFD | `0` | 🔴 |
| `0x889d` | IFD0 + ExifIFD | `"Madrid"`（相机功能代号） | 🔴 |
| `0x889f` | ExifIFD | `0` | 🔴 |
| `0x88a2` | IFD0 | 浮点数组（传感器数据） | 🔴 |
| `0x88a3` | IFD0 | `{}` | 🔴 |


#### 0x889e JSON 内容

```json
{
  "buildDevice": "popsicle",
  "customize": "",
  "livephotoInfo": "tailShakeI:97 head:14447250877 time:14449050877 tail:14450590877
                    filterId:66048 onlyPreDuration:false  offset:266752 tailOffset:934708 ",
  "version": 2,
  "waterLogo": "XIAOMI",
  "waterName": "17 Pro Max"
}

```
| 字段 | 说明 | 与实况相关？ |
|------|------|:---:|
| `buildDevice` | 设备代号 | 否 |
| `customize` | 自定义标记（旧版为 `cvLensName` + `filterName`） | 否 |
| `livephotoInfo` | 拍摄元数据（`head`/`time`/`tail` 为系统时钟值，`offset`/`tailOffset` 为字节偏移）。合成时可填 0 占位 | 否 |
| `version` | JSON 格式版本 | 否 |
| `waterLogo` / `waterName` | 水印标识 / 设备名 | 否 |

> 🔴 `livephotoInfo` 中 `tailShakeI`、`headShakeI`、`onlyPreDuration` 字段用途待确认，与实况照片协议无关。


#### JPEG 私有段

| 段标记 | 大小 | 内容 | 与实况相关？ |
|--------|------|------|:---:|
| APP3 `ALGO_COMMON` | ~20-24 KB | ISP 算法处理数据 | 否 |
| APP3 `OISINFO` | ~0.2-8 KB | OIS 光学防抖数据 | 否 |
| APP4 `XIAOMI_CUSTOMIZE` | ~640-770 B | 私有 EXIF 标签值冗余存储（JSON） | 否 |
| APP5 `CLOUD_WATER` | ~450 B | 🔴 用途待确认 | 🔴 |
| APP5 `XMI Camera Data` | 2 段，~65KB + 变长 | zlib 压缩相机数据 | 否 |

> APP3/APP4/APP5 各段均为相机 ISP 管线数据，与实况照片协议无关。


#### 实用性结论

合成小米可用的实况照片**使用 Google MotionPhoto V2 协议即可**。小米相册不限制读取自家元数据，符合 V2 标准的其他品牌实况照片均可播放。如需小米相册完整显示型号和水印，额外写入 `Make`、`Model`、`0x889e` 三个字段即可。

---

### OPPO O-Live Photo

OPPO / OnePlus 在 Google MotionPhoto V2 基础上扩展的私有协议。完全兼容 V2，同时增加 OPPO 独有字段。使用者：OPPO ColorOS、OnePlus OxygenOS。


#### 二进制结构（OPPO 原厂文件）

OPPO 原厂文件比标准 V2 多了一个尾部区域：

```
┌──────────────────────┐
│   JPEG 图片           │
├──────────────────────┤
│   GainMap 增益图      │  ← HDR 相关，非实况照片协议内容
├──────────────────────┤
│   MP4 视频            │
├──────────────────────┤
│   OnePlus 尾部数据    │  ← 仅 OnePlus 机型，约 846 KB
└──────────────────────┘

```
人工合成的文件通常不含 OnePlus 尾部。


#### XMP 元数据

在 V2 基础上新增 `OpCamera` 命名空间，增加 5 个属性：

```xml
<rdf:Description rdf:about=""
    xmlns:GCamera="http://ns.google.com/photos/1.0/camera/"
    xmlns:Container="http://ns.google.com/photos/1.0/container/"
    xmlns:Item="http://ns.google.com/photos/1.0/container/item/"
    xmlns:OpCamera="http://ns.oplus.com/photos/1.0/camera/"
    GCamera:MotionPhoto="1"
    GCamera:MotionPhotoVersion="1"
    GCamera:MotionPhotoPresentationTimestampUs="2762000"
    OpCamera:MotionPhotoOwner="oplus"
    OpCamera:OLivePhotoVersion="2"
    OpCamera:VideoLength="8275393"
    OpCamera:MotionPhotoPrimaryPresentationTimestampUs="1466388"
    OpCamera:MotionPhotoEnable="True">
  <Container:Directory>
    <rdf:Seq>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Mime="image/jpeg" Item:Semantic="Primary"
                        Item:Length="0" Item:Padding="0"/>
      </rdf:li>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Mime="video/mp4" Item:Semantic="MotionPhoto"
                        Item:Length="9121768" Item:Padding="0"/>
      </rdf:li>
    </rdf:Seq>
  </Container:Directory>
</rdf:Description>

```
> Container:Directory 结构与 V2 一致——Primary 和 MotionPhoto 两个 Item。实际样本中 OPPO 文件可能包含第三个 GainMap Item（HDR 增益图），但与实况照片协议无关。


#### OpCamera 新增字段

| 字段 | 说明 |
|------|------|
| `OpCamera:MotionPhotoOwner` | 所有者标识，固定为 `"oplus"`。OPPO 相册据此判断是否为自家拍摄 |
| `OpCamera:OLivePhotoVersion` | O-Live 协议版本，当前为 `"2"` |
| `OpCamera:VideoLength` | 纯视频字节数（不含 OnePlus 尾部）。原厂文件中 `Item:Length` = 视频+尾部，`VideoLength` = 纯视频。合成时两者相等 |
| `OpCamera:MotionPhotoPrimaryPresentationTimestampUs` | 主照片（原始拍摄帧）的微秒时间戳。原厂文件中与 `MotionPhotoPresentationTimestampUs` 相同；更换封面后，前者保持原值不变（指向原始拍摄帧），后者更新为新封面位置。两者分别记录"拍摄的那一帧"和"当前封面帧" |
| `OpCamera:MotionPhotoEnable` | 封面编辑标记。原厂文件无此字段；在 OPPO 相册中编辑封面后写入，值为 `True`。可用于判断封面是否被用户修改过 |


#### 封面帧机制

OPPO 是唯一拥有**双时间戳**的协议，可区分"当前封面帧"和"原始拍摄帧"：

| 字段 | 含义 | 换封面后 |
|------|------|----------|
| `GCamera:MotionPhotoPresentationTimestampUs` | 当前封面帧在视频中的微秒位置 | **更新**为新封面位置 |
| `OpCamera:MotionPhotoPrimaryPresentationTimestampUs` | 原始拍摄帧（按下快门那一刻）的微秒位置 | **保持不变** |

原厂文件中两者相同。更换封面后，第一个字段变为新封面位置，第二个字段保留原始值。这是其他协议不具备的能力——可以同时知道"现在的封面在哪"和"当初拍摄的那一帧在哪"。


#### EXIF UserComment 标记

除 XMP 外，OPPO 相册还会检查 EXIF 中的 `UserComment` 字段：

```
UserComment = "oplus_10485792"
              │      │
              │      └── 最大视频字节数（10 MB）
              └── OPPO 前缀标识

```
不同机型该值可能不同：8388608 (8 MB)、10485792 (10 MB) 均有过出现。

若缺少此标记，OPPO 自带相册不会播放动态效果。Google Photos 和 Windows 11 不受影响——仅检查 XMP。

---

### <a id="vivo-x300"></a>vivo Live Photo（X300 系列起）

> **逆向依据**：`C:\Users\LengxiQwQ\Downloads\实况照片样本\vivo实况照片样本\`（5 个 vivo X300 Ultra 相机直出样本）。
> 本节以真实文件为准，与早期文档结论（"用 Google V2 即可"）不同。

vivo X300 系列的单文件实况照片**不是**纯 Google MotionPhoto V2，而是带 **Ultra HDR 增益图（hdrgm）** 的三段式容器，并写入非空 EXIF UserComment 记录相机处理状态。只写 V2 两段式 XMP 的文件 vivo 相册**无法识别**为实况照片。

#### 二进制结构

```
┌──────────────────────┐
│   Primary JPEG       │   ← 主图（含 EXIF + XMP），Item:Semantic="Primary"
├──────────────────────┤
│   GainMap JPEG       │   ← Ultra HDR 增益图（完整第二个 JPEG，FFD8…FFD9）
├──────────────────────┤
│   MP4 视频            │   ← HEVC/H.265，视频定位按 V2 Item:Length
└──────────────────────┘
```

真实样本（vivo.jpg，8.27 MB）三段实测长度：Primary `2001908` B + GainMap `477680` B + Video `5790734` B。

#### XMP 元数据（真实 vivo.jpg 原文）

```xml
<rdf:Description rdf:about=""
    xmlns:hdrgm="http://ns.adobe.com/hdr-gain-map/1.0/"
    xmlns:Container="http://ns.google.com/photos/1.0/container/"
    xmlns:Item="http://ns.google.com/photos/1.0/container/item/"
    xmlns:GCamera="http://ns.google.com/photos/1.0/camera/"
    xmlns:VCamera="http://ns.vivo.com/photos/1.0/camera/"
  hdrgm:Version="1.0"
  GCamera:MotionPhoto="1"
  GCamera:MotionPhotoVersion="1"
  GCamera:MotionPhotoPresentationTimestampUs="1450273"
  VCamera:VMotionPhotoVersion="1"
  VCamera:VMotionPhotoSource="1"
  VCamera:VMediaKitVersion="1.0.0.9">
  <Container:Directory>
    <rdf:Seq>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Semantic="Primary" Item:Mime="image/jpeg"/>
      </rdf:li>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Semantic="GainMap" Item:Mime="image/jpeg" Item:Length="477680"/>
      </rdf:li>
      <rdf:li rdf:parseType="Resource">
        <Container:Item Item:Mime="video/mp4" Item:Semantic="MotionPhoto" Item:Length="5790734" Item:Padding="0"/>
      </rdf:li>
    </rdf:Seq>
  </Container:Directory>
</rdf:Description>
```

与 Google V2 的关键差异：

| 项 | Google V2 | vivo X300 真实文件 |
|------|-----------|------|
| 命名空间 | GCamera/Container/Item | + **hdrgm**（`ns.adobe.com/hdr-gain-map/1.0/`）+ **VCamera** |
| `hdrgm:Version` | 无 | `"1.0"` |
| Container item 数 | 2（Primary / MotionPhoto） | **3**（Primary / **GainMap** / MotionPhoto） |
| Primary item 属性 | `Item:Mime` + `Item:Length="0"` + `Item:Padding` | 仅 `Item:Semantic` + `Item:Mime`（**无 Length/Padding**） |
| GainMap item | 无 | `Semantic="GainMap"` + `Mime="image/jpeg"` + `Length=<增益图字节>` |
| MotionPhoto item | Mime/Semantic/Length/Padding | 同 V2（`Length` = 视频字节数，`Padding="0"`） |
| `MotionPhotoPresentationTimestampUs` | 通常 0 | 非 0（样本 821906~1513060） |

属性顺序：Primary/GainMap 为 `Item:Semantic` 在前；MotionPhoto 为 `Item:Mime` 在前（拆分正则两种顺序均已兼容）。

#### VCamera 字段

| 字段 | 说明 |
|------|------|
| `VCamera:VMotionPhotoVersion` | vivo 动态照片协议版本，当前为 `1` |
| `VCamera:VMotionPhotoSource` | 来源标识，`1` = 相机直出 |
| `VCamera:VMediaKitVersion` | 媒体处理引擎版本号（样本 `1.0.0.9`） |

VCamera 字段是 vivo 的辅助身份标记（App 检测器据此识别 vivo 文件），但**不足以**让 vivo 相册把合成文件识别为实况照片——三段式 hdrgm 结构与 UserComment 同样必要。

#### EXIF UserComment（关键）

真实样本**必带非空 UserComment**（590 字节左右），记录相机处理状态，纯 UTF-8 无标准 8 字节编码前缀、尾部 `\x00` 结束。各字段组之间以 `\n`（0x0A）分隔（exiftool 文本显示会把 `\n` 渲染成 `.`）。跨 5 个样本不变的实况关键字段：

```
multi-frame: 1;                       // 单帧实况判定标志
ispap:1;                              // 相机 ISP 流水线已处理
papproctime: <yyyy:MM:dd HH:mm:ss>    // 拍摄处理时间
module: photo;                        // 拍摄模式
```

**UserComment 按整段全量复刻**，作为 vivo 相册的私有识别签名：纯 Google V2（无 UserComment）无法被 vivo 相册识别，且机制与 OPPO 认 `oplus_` 标记类似——内容不完整可能导致整段不被读取。因此软件合成输出**完整复刻真实样本的整段字段结构**（`filter: 2237; niceRunStatus: 1002; … multi-frame: 1; … ispap:1; … zeissColor: bright;`，`\n` 分隔共 11 行，首行后无空格分隔），仅 `papproctime` 填当前时间、其余逐拍变化的相机状态值（weather/AEC/温度等）用中性值合成——每张真实拍摄这些值本就不同，识别只依赖固定的实况签名字段与非空整段。

⚠️ 已知差异（待真机验证）：真实样本为**无前缀裸 UTF-8**；软件合成经 exiftool 写入，带标准 8 字节 `ASCII\0\0\0` 编码前缀（内容完整、`\n` 保留）。若 vivo 相册严格按字节比对前缀，需改为自研 EXIF 写入（当前依赖 exiftool）；此点与「XMP 结构 / 2-item 无 HDR」并列为真机测试的第一排查项。

#### 实用性结论

合成 vivo X300 兼容的实况照片遵循原则：**XMP 只写与实况照片相关的元数据，与实况无关的一律不写；唯一例外是 HDR——源图自带 HDR 则原样照搬，没有则不写。EXIF UserComment 是另一个例外：作为 vivo 相册的私有识别签名，按真实样本整段全量复刻（见下）。**

1. **实况核心元数据**（必有）：GCamera（`MotionPhoto="1"`、`MotionPhotoVersion`、`MotionPhotoPresentationTimestampUs`）、VCamera 身份字段、Container:Directory 的 Primary + MotionPhoto item（Primary 无 Length/Padding，MotionPhoto `Item:Length` = 视频字节数）；
2. **非空 EXIF UserComment**（必有）：**整段全量复刻**真实样本的字段结构（`\n` 分隔共 11 行，含 `multi-frame: 1` / `module: photo` / `ispap:1` / 实时 `papproctime` 等实况签名字段），作为 vivo 相册私有识别签名；逐拍变化的相机状态值（weather/AEC/温度等）用中性值合成；
3. **HDR（条件）**：源图带 Ultra HDR 增益图时，输出 `[SOI+XMP][Primary][GainMap][Video]`，XMP 含 hdrgm 命名空间 + `hdrgm:Version="1.0"` + GainMap item（`Item:Length` = 实际增益图字节数，与字节一一对应），增益图原样保留；源图无增益图（普通 JPEG）时，不写 hdrgm、不写 GainMap item，Container 为 2-item（Primary + MotionPhoto）。

---

### 华为 Moving Photo

华为实况照片协议完全独立于 Google/OPPO/Apple 标准，**不使用 XMP 元数据**，通过私有二进制标记来标识和定位视频数据。两种容器格式——HEIC 和 JPEG——共用同一套尾部结构和 MP4 元数据。

**最小可用结构**（SDK 简化格式，已验证）：
```
[静态图片 JPEG/HEIC] + [完整 MP4 视频] + [60 字节尾部]
```

---

#### 容器结构概述

**HEIC**：静态图片（多分辨率 HEVC）+ 嵌入 MP4 + 尾部。定位 MP4：搜索第二个 `ftyp` box（major=`mp42`）。

**JPEG**：JPEG 图片 + 嵌入 MP4 + 尾部。定位 MP4：搜索 `ftyp` box（在 JPEG EOI 之后）。

---

#### 实况照片判定：LIVE_ 尾标

`LIVE_` 是实况照片的**唯一判定标记**。相册通过扫描文件末尾的 `LIVE_` 字节序列来识别实况照片。其他标记（tmap、Track 3、com.openharmony.* 键等）均非必需。

---

#### 60 字节尾部结构

```
v6_fXX            PPP:QQQQ            LIVE_NNNNNNN
```

| 偏移 | 字节 | 字段 | 含义 |
|------|:---:|------|------|
| [+0..+5] | 6 | `v6_fXX` | 封面帧序号（XX = 视频中帧号，0-based），供**旧版相册**读取 |
| [+6..+19] | 14 | 空格 | — |
| [+20..+27] | 8 | `PPP:QQQQ` | 只读历史记录（合成样本为 `帧号:总帧数`，真机如 `700:1300`，不建议据此换算秒） |
| [+28..+39] | 12 | 空格 | — |
| [+40..+51] | 12 | `LIVE_NNNNNNN` | **实况照片判定标记**，NNNNNNN = 嵌入 MP4 字节数 **+20** |
| [+52..+59] | 8 | 空格 | — |

> **LIVE_ 公式**：`嵌入 MP4 字节数 + 20`。经华为手机实测验证，+16 不可用（相册不识别），必须使用 +20。

---

#### 封面帧控制（双机制）

**华为相册有两个版本，读取不同的封面帧位置**：

| 相册版本 | 读取位置 | 格式 | 写入方式 |
|----------|----------|------|----------|
| **新版**（HarmonyOS 5+） | MP4 udta `com.openharmony.covertime` | 真机为 4 字节 **float32**（毫秒，如 `0x44A28B44 ≈ 1300.35`）；本软件合成写 UTF-8 字符串（如 `"1330.000000"`） | ffmpeg `-movflags use_metadata_tags` 注入 |
| **旧版** | 尾部 `v6_fXX` | 帧序号（整数，如 `29`） | 直接写入尾部字节 |

> **关键发现**：经实测，修改 `v6_fXX` 对最新版相册**无影响**——封面位置不变。只有写入 `com.openharmony.covertime` 到 MP4 udta 元数据后，封面位置才发生变化。`v6_fXX` 仅为旧版相册兼容保留。

**封面帧替换流程**（用户在相册中编辑封面时）：
1. 从视频中抽取选定帧 → 编码为 JPEG/HEIC → 替换文件前半段静态图
2. 视频完整保留（不裁切）
3. 更新 `com.openharmony.covertime`（新相册）和 `v6_fXX`（旧相册）
4. 更新 `LIVE_` = 新 MP4 字节数 + 20
5. `PPP:QQQQ` 保持原值不变（只读历史记录）

---

#### 嵌入 MP4 视频

**ftyp**：`major=mp42`, `compat=iso2, mp42`

**最少 2 个轨道**即可正常播放：

| Track | 类型 | 编码 | 说明 |
|-------|------|------|------|
| Track 1 | 视频 | H.264 (avc1) | **必须 H.264**——HEVC 编码的视频华为相册无法播放 |
| Track 2 | 音频 | AAC | 44100Hz 或 48000Hz |

> Track 3（`com.openharmony.timed_metadata.movingphoto`）为原生相机自动附加，**非必需**。

**MP4 udta 元数据**：

| Key | 说明 | 必需？ |
|-----|------|:---:|
| `©too` = `"Openharmony6.1"` | 编码器标识 | 辅助 |
| `com.openharmony.covertime` | **封面帧毫秒时间戳**。真机 `ilst` 用 type `0x17` 存 float32；合成端可写 UTF-8 字符串 | ✅ **新版相册必需** |
| 其他 `com.openharmony.*` 键 | 拍摄元数据（encParam、starttime、videoId 等） | 否 |

---

#### EXIF 辅助标记

| 字段 | 值 | 说明 |
|------|-----|------|
| Make | 保留来源值（方案 A） | 合成时不再改写为 `HUAWEI`，实况照片判定不依赖 Make；后备方案 B 详见 `LivePhotoMergeService.WriteHuaweiNativeAsync` 方法头注释 |
| MakerNote | `##**N4031` | 原生相机写入，非必需 |

---

#### 关键参数速查

| 参数 | 值 |
|------|-----|
| 容器格式 | HEIC 或 JPEG |
| 尾部大小 | **60 字节** |
| LIVE_ 公式 | MP4 字节数 **+20** |
| 封面帧-新版 | MP4 udta `com.openharmony.covertime`（毫秒） |
| 封面帧-旧版 | 尾部 `v6_fXX`（帧序号） |
| PPP:QQQQ | 只读历史记录（真机格式不固定，不要据此换算秒） |
| 视频编码 | **H.264**（HEVC 不可用） |
| 视频 ftyp brand | `mp42` |
| 最小轨道 | Video + Audio（2 轨） |
| HEIC ftyp | 最后 4 字节替换为 `tmap`（辅助，非必需） |

---

#### 提取逻辑

**提取静态图片**（封面帧）：
- HEIC：解析 ISOBMFF → `meta` → `iloc` → 提取全分辨率 HEVC → 转 JPEG/PNG
- JPEG：读取到第一个 `FF D9`（JPEG EOI）之前的数据即图片

**提取视频**：
```
mp4_start = 第二个 ftyp (major=mp42) 的位置 - 4   // HEIC
mp4_start = JPEG EOI 之后的 ftyp 位置 - 4          // JPEG
video = data[mp4_start : len(data) - 60]           // 减去尾部
```

---

#### 合成实况照片（操作指南）

```
结构:  [静态图 JPEG/HEIC] + [完整 MP4] + [60B 尾部]

60B 尾部字段:
  [+0..+5]:   v6_fXX      (封面帧序号，旧版相册)
  [+20..+27]: PPP:QQQQ    (封面帧号:总帧数，只读记录)
  [+40..+51]: LIVE_NNNNN  (MP4字节数 + 20)

MP4 元数据:
  com.openharmony.covertime = "{StillImageTime_ms}.000000"  (新版相册封面位置)
  too = "Openharmony6.1"                                     (编码器标识)
```

#### 更换封面帧

```
1. 从视频抽取目标帧 → 编码为 JPEG/HEIC
2. 替换文件前半段静态图片
3. 视频保留完整（不裁切）
4. 更新 com.openharmony.covertime = 新帧的时间戳（毫秒）
5. 更新 v6_fXX = 新帧序号
6. 更新 LIVE_ = 新 MP4 字节数 + 20
7. PPP:QQQQ 保持原值不变
```

---

### 荣耀 Moving Photo

荣耀 Magic 系列（BKQ-AN10 等）的动态照片与华为使用**同一套协议**：JPEG 中段嵌入 MP4 + `LIVE_` 尾标。实况照片的判定、视频定位、拆分逻辑与华为完全一致。

与华为唯一的区别在于尾部结构——荣耀在标准华为尾部（真机荣耀用 `v2_fXX`，华为用 `v6_fXX`，`PPP:QQQQ`，`LIVE_NNNNNNN`）**前面**追加了一段可变长的逐帧防抖矩阵 JSON：

```
uuidextend_type_matrix[{"frameNum":1,"matrixInfo":"{...}"}, ...]
v2_f43              499:1000            LIVE_7054584
└── 荣耀扩展（非实况）──┘└── 标准华为尾部 ──┘

```
JSON 中每个元素对应一个视频帧，记录该帧的 EIS（电子防抖）和 MCTF（运动补偿时域滤波）的 3×3 变换矩阵及画面裁剪参数：

```json
{
  "frameNum": 1,
  "matrixInfo": "{\"decayGain\":1.0,
    \"eis3x3Matrix\":[0.999,-0.043,-41.5,0.043,0.999,17.4,...],
    \"mctf3x3Matrix\":[1.010,-0.008,-125.3,0.009,1.008,161.3,...],
    \"srcDstWh\":[1920,1440,1728,1296]}"
}

```
| 标记 | 说明 | 与实况相关？ |
|------|------|:---:|
| `uuidextend_type_matrix` | 荣耀私有扩展段标识 | 否，视频后处理数据 |
| JSON 内 `eis3x3Matrix` | 逐帧电子防抖矩阵 | 否 |
| JSON 内 `mctf3x3Matrix` | 逐帧运动补偿矩阵 | 否 |
| `v2_fXX`（荣耀）/ `v6_fXX`（华为） / `PPP:QQQQ` / `LIVE_` | 标准华为尾部字段，含义与华为一致 | ✅ |
| `LIVE_NNNNNNN` | 实况照片判定标记，与华为完全一致 | ✅ |

> 以上 JSON 扩展为荣耀相机 ISP 管线输出的逐帧后处理数据，与实况照片协议无关。仅 `LIVE_` 是实况照片判定标记。

---

### 三星 Motion Photo

三星 Galaxy 设备的动态照片使用 **双轨制元数据**——同时写入 Google V2 XMP（兼容 Google Photos 等第三方 App）和私有二进制 **Samsung Trailer**（供三星相册使用）。三星相册**完全不读取 XMP**，仅依赖 Trailer 中的 `MotionPhoto_Data` 标签和 SEF 验证区来识别和播放动态照片。

使用者：三星 Galaxy S23 系列及以后、Galaxy Tab S9 系列等。内部格式自称为 **mpv2**（早期）或 **mpv3**（Galaxy S23 Ultra 起）。


#### JPEG 二进制结构

与 Google V2 的关键区别：视频**嵌入在** Samsung Trailer 的 `MotionPhoto_Data` 标签 data 中，而非作为独立数据段追加在 JPEG EOI 之后：

```
┌──────────────────────────────────────┐
│   JPEG 图片                           │
│   (含 V2 XMP: GCamera:MotionPhoto=1   │
│    + Container:Directory)             │
├──────────────────────────────────────┤
│   Samsung Trailer 标签区               │
│   ├─ Image_UTC_Data       (0x0a01)   │
│   ├─ MCC_Data             (0x0aa1)   │
│   ├─ Camera_Scene_Info    (0x0d01)   │
│   ├─ Color_Display_P3     (0x0cc1)   │
│   ├─ Camera_Capture_Mode  (0x0c61)   │
│   ├─ MotionPhoto_Data     (0x0a30)   │  ← MP4 视频在此
│   └─ MotionPhoto_Version  (0x0a31)   │  ← "mpv2" 或 "mpv3"
├──────────────────────────────────────┤
│   SEF 验证区                          │
│   ├─ "SEFH" header                   │
│   ├─ 每个 Tag 的索引条目（offset+size）│
│   └─ "SEFT" footer                   │
└──────────────────────────────────────┘

```

> 整个 Trailer = 标签区 + SEF 区（含视频数据），追加在 JPEG EOI 之后、文件末尾。视频不是独立的顶层数据段——它是 `MotionPhoto_Data` 标签的 data payload。

#### 每个 Tag 的二进制编码

Samsung Trailer 中每个 Tag 的通用编码格式为：

```
偏移    长度    内容
+0      2       0x00 0x00（固定前导）
+2      2       Marker ID（小端序 uint16，如 0x0a30 → 字节 [0x30, 0x0a]）
+4      4       Name 字符串长度（小端序 uint32，含末尾 NUL）
+8      N       Name 字符串（UTF-8，如 "MotionPhoto_Data"）
+8+N    M       Data（M = 该 Tag 的数据长度，由 SEF 索引中的 size 字段给出）
```

例——`MotionPhoto_Data` 标签（JPEG 容器）的二进制编码：
```
00 00  30 0A  10 00 00 00  "MotionPhoto_Data"  <MP4 视频完整二进制>
```


#### 完整 Tag 清单（7 个，按文件内出现顺序排列）

| # | Marker (hex) | Name | 数据内容 | 说明 |
|---|:---:|---|---|---|
| 1 | `0x0a01` | `Image_UTC_Data` | Unix 时间戳（LE u32, 4 字节） | 拍摄时间，无时区信息 |
| 2 | `0x0aa1` | `MCC_Data` | 变长二进制 | 用途未完全文档化（可能与移动国家代码相关） |
| 3 | `0x0d01` | `Camera_Scene_Info` | 变长二进制 | 相机场景信息 |
| 4 | `0x0cc1` | `Color_Display_P3` | 变长二进制 | 广色域 Display P3 色彩标记 |
| 5 | `0x0c61` | `Camera_Capture_Mode_Info` | 变长二进制 | 拍摄模式信息 |
| 6 | `0x0a30` | `MotionPhoto_Data` | **JPEG**: 完整 MP4 视频字节<br>**HEIC**: `"mpv2"` + video_offset(BE u32) + video_size(BE u32) | ★ 核心标签——三星相册视此标签存在与否判定是否为动态照片 |
| 7 | `0x0a31` | `MotionPhoto_Version` | 版本字符串，如 `"mpv2"` 或 `"mpv3"` | 格式版本标识 |


> **ExifTool 映射**：ExifTool 将 marker `0x0a30` 抽象为两个逻辑字段——`EmbeddedVideoType`（值为 name 字符串 `"MotionPhoto_Data"`）和 `EmbeddedVideoFile`（值为 data 部分的视频字节）。但底层二进制仅有 marker + name + data 三段，不存在独立的 "type" 和 "file" 分离存储。

#### SEF 验证区结构

SEF（Samsung Extended Format）位于所有 Tag 之后，是一个自描述索引区。三星相册通过 SEF 快速定位各 Tag 的偏移和长度——**缺少 SEF 区或 SEF 区数据不一致，三星相册将拒绝识别**。

```
偏移        长度        内容
+0          4           "SEFH"（魔数）
+4          4           Version（LE u32，固定为 107）
+8          4           Field Count（LE u32，= 实际写入的 Tag 数量，通常为 7）
+12         N×10        每个 Field 的索引条目：
                        [2B: 0x00 0x00] [2B: marker LE u16] [4B: offset_from_sef LE u32] [4B: size LE u32]
+12+N×10    4           SEF 总大小（LE u32，含此字段自身和 "SEFT"）
+16+N×10    4           "SEFT"（魔数）
```

各 Field 的 `offset_from_sef` 以 SEF 区起始位置（即 "SEFH" 的 +0 字节）为基准——Tag 中偏移值为 0 表示该 Tag 紧接在 SEFT 之后。

#### 与 Google V2 的兼容性（双轨制）

三星原厂文件会**同时写入**两套独立的元数据：

| 层 | 格式 | 用途 |
|---|------|------|
| **XMP** | Google MotionPhoto V2（`GCamera:MotionPhoto=1` + `Container:Directory`） | 兼容 Google Photos、Windows 资源管理器等第三方 App |
| **Trailer** | Samsung Private Trailer（7 Tags + SEFH/SEFT） | **三星相册唯一识别方式** |

关键事实：
- **三星相册完全不读取 XMP**，仅凭 Trailer 判定动态照片
- **Google Photos 不读取 Samsung Trailer**，仅凭 V2 XMP 判定
- 合成三星兼容的动态照片**必须同时写入两套元数据**，缺一不可

#### Motion Photo v3（mpv3）

较新的三星设备（Galaxy S23 Ultra、Tab S9 等）在 `MotionPhoto_Version` 标签中写入 `"mpv3"` 而非 `"mpv2"`。两种版本的 Trailer 和 SEF 结构完全一致，仅版本字符串不同。MotionPhoto2 工具默认生成 `"mpv3"` 格式。

#### HEIC 容器：mpvd 与 sefd box

当容器为 HEIC 时，三星不使用 JPEG Trailer 追加方式，而是通过两个私有 **ISOBMFF 顶层 box** 封装动态照片数据：

```
┌──────────────────────────────────────┐
│   HEIC 静态图片 (ftyp + meta + mdat)  │
│   (meta box 内含 V2 XMP)              │
├──────────────────────────────────────┤
│   mpvd box (Motion Photo Video Data) │
│   ├─ 4B: box size (BE u32)           │
│   ├─ 4B: "mpvd"                      │
│   ├─ MP4 视频原始字节                  │
│   └─ sefd box (嵌套在 mpvd 内部)      │
│       ├─ 4B: box size (BE u32)       │
│       ├─ 4B: "sefd"                  │
│       ├─ Tag 数据区（格式同 JPEG，     │
│       │   但 MotionPhoto_Data 不含视频）│
│       └─ SEFH/SEFT 验证区             │
└──────────────────────────────────────┘

```

**HEIC 与 JPEG 的关键区别**：

| 方面 | JPEG | HEIC |
|------|------|------|
| 视频位置 | 在 Trailer 的 `MotionPhoto_Data` data 中 | 在 mpvd box 中（sefd 之前） |
| `MotionPhoto_Data` 内容 | 完整 MP4 视频字节 | `"mpv2"` + video_offset(BE u32) + video_size(BE u32)（12 字节指针） |
| 视频偏移基准 | SEF 区起始（"SEFH" 位置） | mpvd box 起始 |
| 容器级标识 | 无 | mpvd + sefd box 名 |
| XMP Primary Padding | 0 | 8（= mpvd 头部 8 字节） |

> HEIC 下的 `MotionPhoto_Data` data 为 `"mpv2"`（4 字节）+ 视频在 mpvd box 内的字节偏移（BE u32）+ 视频字节长度（BE u32），共 12 字节。三星相册据此在 mpvd box 内定位视频数据。


#### 提取逻辑

1. 使用 ExifTool 提取视频：`exiftool -b -EmbeddedVideoFile > video.mp4`
   （ExifTool 自动解析 Samsung Trailer，抽取 `0x0a30` 标签的 data 部分）
2. 使用 ExifTool 提取完整 Trailer：`exiftool -b -trailer > out.bin`
3. 手动替换视频需同步更新：
   - `MotionPhoto_Data` 的 data 部分（新视频字节）
   - SEF 索引中 `0x0a30` 条目的 size 字段
   - 所有后续 Tag 的 offset 字段
   - 简单截断并追加新 MP4 **会被三星相册拒绝**
4. HEIC 文件中的提取：搜索 `"mpvd"` box → 解析内部 sefd → 根据 `MotionPhoto_Data` 中的 offset/size 指针定位视频


#### 合成实用性结论

- 合成三星兼容的动态照片需要**同时满足**：
  - **Samsung Trailer**（7 Tags + SEFH/SEFT）——供三星相册识别
  - **Google MotionPhoto V2 XMP**（`GCamera:MotionPhoto=1` + `Container:Directory`）——供 Google Photos 等第三方 App 识别
- Google Gallery 额外要求文件名以 `"MV"` 开头
- 参考实现：[PetrVys/MotionPhoto2](https://github.com/PetrVys/MotionPhoto2)（Python，完整处理 JPEG 和 HEIC 两种容器，默认生成 mpv3）
- Trailer 内部偏移量和 SEF 索引有严格的交叉引用关系，手动拼接极易出错，推荐程序化生成


#### 视频定位方式对比

| 协议 | 视频定位方式 | 标记技术 |
|------|-------------|----------|
| Google V1 | 从 EOF 前推 `MicroVideoOffset` 字节 | XMP 属性 |
| Google V2 | `Container:Directory` 第 2 个 Item 的 `Item:Length` | XMP 结构化目录 |
| OPPO O-Live | 同 V2，`OpCamera:VideoLength` 为辅 | XMP 属性 |
| 小米 Mi-Live | 同 V2 | XMP 属性（`MiCamera:XMPMeta` / `video_madrid_image`） |
| vivo Live | 同 V2 | 3-item Ultra HDR XMP（hdrgm）+ `VCamera` + EXIF UserComment |
| 三星 Motion | XMP `Item:Length`（Google Photos）+ SEF 索引 `0x0a30` size（三星相册） | XMP + 私有二进制 Trailer |
| Apple Live | 视频为独立 MOV 文件 | ContentIdentifier UUID |
| 华为 Moving（HEIC） | 嵌入 MP4 ftyp box（搜索第二个 `ftyp`, major=`mp42`） | 私有 ISOBMFF 盒子解析 |
| 华为 Moving（JPEG） | 嵌入 MP4 ftyp box（搜索 `ftypmp42` 字节序列） | 私有二进制偏移量 + `LIVE_` 尾标 |
| 荣耀 Moving | 同华为 JPEG | 私有二进制偏移量 + `LIVE_` 尾标 |


#### 元数据策略对比

| | Google | OPPO | 三星 | Apple | 华为 | 荣耀 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| XMP 标准元数据 | ✅ GCamera | ✅ GCamera + OpCamera | ✅ GCamera MotionPhoto V2（兼容 Google Photos） | ❌ | ❌ | ❌ |
| 私有二进制标记 | ❌ | ✅ EXIF UserComment | ✅ Samsung Trailer（7 Tags + SEFH/SEFT）+ mpvd/sefd box | ✅ QuickTime 元数据 | ✅ LIVE_ + covertime | ✅ LIVE_ |
| 容器级品牌标识 | ❌ | ❌ | ✅ ISOBMFF: mpvd/sefd (HEIC) | ✅ ftyp=mif1 | ⚠️ ftyp=tmap（非实况专属） | ❌ |
| 跨平台兼容性 | ✅ 广泛 | ✅ 广泛 | ⚠️ Google Photos 兼容（需 V2 XMP） | ⚠️ Apple 生态 | ⚠️ 仅华为/荣耀生态 | ⚠️ 仅荣耀/华为生态 |
| HDR 支持 | ⚠️ 取决于 JPEG | ⚠️ 取决于 JPEG | ⚠️ 取决于 JPEG/HEIC | ✅ Dolby Vision/HLG | ✅ BT.2100 HLG + ICC Profile | — |
| 规范透明度 | ✅ 公开 | ⚠️ 部分公开 | ❌ 封闭（Trailer 未公开，社区逆向） | ❌ 封闭 | ⚠️ SDK API 公开 | ❌ 封闭 |
| 官方合成 API | ❌ | ❌ | ❌ | ❌ | LivePhotoKit / MediaLibraryKit | ❌ |


---

## 📁 双文件格式


### Apple Live Photo


#### 文件结构

iPhone 拍摄的实况照片由两个独立文件组成：

```
IMG_1234.HEIC  — 静态照片（HEIF 容器，含封面帧图像；JPG 同样可行）
IMG_1234.MOV   — 动态视频（QuickTime 容器，约 3 秒，含音频轨）

```

#### 配对机制：ContentIdentifier

两个文件存储相同的 `ContentIdentifier` UUID。此 UUID 由 iPhone 在拍摄时生成，写入 QuickTime 顶级元数据：

```
IMG_1234.HEIC → ContentIdentifier = "D4E5F6A7-89AB-4CDE-..."
IMG_1234.MOV  → ContentIdentifier = "D4E5F6A7-89AB-4CDE-..."  （完全一致）

```
配对优先级：ContentIdentifier UUID > 文件名 > 拍摄日期+GPS 兜底。


#### HEIC 文件中的实况照片相关元数据

| 元数据字段 | 位置 | 说明 |
|-----------|------|------|
| `ContentIdentifier` | MakerNotes (QuickTime) | **配对 UUID**，与 MOV 中的一致 |
| `QuickTime:Rotation` | QuickTime 元数据 | 旋转方向（如 `Rotate 90 CW`） |
| `EXIF:Orientation` | EXIF IFD | EXIF 方向标签。可能与 `Rotation` 不一致 |

> `Orientation` 与 `Rotation` 用于修复页面的方向校正，不影响实况照片识别。


#### MOV 文件中的实况照片相关元数据

| 元数据字段 | 位置 | 说明 |
|-----------|------|------|
| `ContentIdentifier` | QuickTime 顶级（moov/meta/ilst） | **配对 UUID**，与照片端的 MakerNote 一致 |
| `ContentDescribes` | meBx 轨（Track 3） | **内容描述元数据轨**，其 stsd keys 里是 `com.apple.quicktime.live-photo-info` |
| `LivePhotoAuto` | QuickTime 顶级 | **实况照片自动标记**，值为 `1` |
| `LivePhotoVitalityScore` | QuickTime 顶级 | 活力评分，0~1 之间 |
| `LivePhotoVitalityScoringVersion` | QuickTime 顶级 | 活力评分算法版本，当前为 `4` |
| `PosterTime` | QuickTime Movie 级别 | 始终为 `0 s`，**不反映真实封面帧位置** |
| `StillImageTime` | 封面轨（Track 4）elst | 空编辑段 `media_time=-1`，其 `TrackDuration` 才是封面帧在视频中的实际秒数 |
| `QuickTime:Matrix` | Movie 级别 | 3×3 仿射变换矩阵（通常 identity） |
| `QuickTime:Matrix` | Track 1 级别 | 视频轨的 3×3 旋转矩阵 |
| `MediaDuration` | Track 1 | 视频时长（秒），通常 2.7~3.0 秒 |

> `LivePhotoAuto`、`LivePhotoVitalityScore`、`LivePhotoVitalityScoringVersion` 为活力评分相关字段，非识别、配对、编辑的前提。


#### MOV 轨道结构

iOS 可识别的最小结构（Next Track ID = 5，即 4 个轨道）：

| Track ID | 类型 | Handler | 说明 |
|:---:|------|------|------|
| 1 | Video Track | Core Media Video | 视频轨，HEVC (hvc1) 编码，含旋转矩阵 |
| 2 | Audio Track | Core Media Audio | 音频轨 |
| 3 | NRT Metadata | Core Media Metadata | 元数据轨，`ContentDescribes` 指向 Track 1 |
| 4 | NRT Metadata | Core Media Metadata | 封面帧轨（mebx），携带 StillImageTime / 封面变换 |

每个媒体轨（Video/Audio/两个 Metadata）的 `minf` 内还各带一个 **Alias Data Handler**（`dhlr`/`alis`/`appl`，名称为 "Core Media Data Handler"）。

> 实拍文件有 11~12 个轨道，含多个 Auxiliary Picture 轨（sky map、person map 等）；4 轨为最小结构，其余轨道与识别、播放、编辑无关。


#### 封面帧机制

与 Google/OPPO 用微秒时间戳指向视频中某一帧不同，Apple 的封面帧位置藏在 MOV 文件的 **Timed Metadata 轨道**（mebx 类型）中。具体机制：

1. MOV 的 Movie 级别 `PosterTime` **始终为 0**，不反映实际封面位置
2. 封面位置由**封面轨（Track 4）的 elst** 表达：第一段是空编辑（`media_time=-1`），其时长 = 封面在视频中的时间点（以 MovieTimeScale 44100 为单位）；第二段是 74 tick 的封面媒体
3. 封面轨的 meBx 载荷是 89 字节固定数据
4. **该轨的 `TrackDuration` 即封面帧在视频时间轴上的位置**（秒）

更换封面帧时，需要更新封面轨 elst 的空编辑时长（封面时间点），封面载荷本身保持不变。


#### 创建 Apple 兼容实况照片的最低要求

iOS 识别一组文件为实况照片需要：

**照片端**（HEIC **或** JPG 均可）：

- Apple MakerNote 中写入 `ContentIdentifier` UUID（仅此一项即可；70 字节最小 MakerNote）
  - MakerNote 必须位于 **ExifIFD**（tag `0x927C`），且该 IFD 中**只能有这一条** `0x927C`。
    写到 IFD0 或同一 ExifIFD 里存在重复 `0x927C` 时，iOS 不会将其识别/导入为实况照片。

> 华为/荣耀原生照片转 Apple 时，其图片 ExifIFD 常自带 1~4 条 `0x927C`
> （如华为 `##**N4021`、荣耀 `AF_C` 等原生 MakerNote）。转换前必须先删除全部旧 `0x927C`，
> 再写入唯一一条 Apple MakerNote；华为 `VLDMAGIC`、荣耀 `BKQ_PRO` 等相机私有数据可原样保留，
> 不影响 iOS 导入。

**视频端**（MOV）：

- `ContentIdentifier` UUID（与照片端一致）
- `Software = "17.0.2"`、`CreationDate`
- 4 轨最小结构：Video + Audio + ContentDescribes(meBx) + 封面(meBx)

#### 容器结构规范

1. 顶部容器顺序为 `ftyp + moov + wide + mdat`，`moov` 前移，单 `mdat`。
   - `ftyp`：major brand `qt  `、minor 0、compatible `qt  `，共 20 字节（缺 compatible 会导致识别异常）。
   - `wide`：8 字节占位，紧跟 moov。
   - `mdat`：视频数据 → 音频数据 → ContentDescribes 数据 → 封面数据，顺序和 stco 一致。

2. 轨道结构为 4 轨：Video(hvc1) + Audio(mp4a) + ContentDescribes(meBx) + 封面(meBx)。
   - 视频轨 `tapt` 必须含 `clef/prof/enof` 三个 20 字节 FullBox（size + type + version/flags + width/height）。
   - 每个媒体轨的 `minf` 内带 Alias Data Handler：`hdlr` 的 componentType=`dhlr`、subtype=`alis`、manufacturer=`appl`。
   - `vmhd` 的 flags=1、graphicsMode=0x40(ditherCopy)、opColor=0x8000×3；Metadata 轨用 `gmhd/gmin`。
   - `hdlr` 的布局是 `version/flags + componentType + componentSubtype + manufacturer + componentFlags + componentFlagsMask + Pascal 名`。

3. `meta` 位于 moov 直属（不是 `udta/meta`），无 version/flags。
   - `hdlr`：componentType=0、subtype=`mdta`、manufacturer=0、空名。
   - `keys`：3 个键，按序为 `com.apple.quicktime.content.identifier / .software / .creationdate`。
   - `ilst`：3 个 item，index 1~3 与 keys 一一对应，data type=1(UTF-8)。

4. 配对只靠 ContentIdentifier：照片端 MakerNote 与 MOV 端 `meta/ilst` 写同一 UUID。
   - 照片端 Apple MakerNote 为 70 字节：`"Apple iOS\0\0\1" + "MM" + 1 条目(tag 0x0011 ContentIdentifier)`。
   - JPG 和 HEIC 均可行。

5. 封面帧在封面轨的 elst 里：封面轨 `elst = [empty_edit(cover_time), cover_media(74 ticks)]`，cover_time 用 MovieTimeScale(44100) 表示；封面 meBx 载荷为 89 字节固定数据。

6. ContentDescribes 轨的 meBx 载荷为 144 字节，每段视频重复 `round((时长-0.05)*60)` 次；其 stsd 里的 keys 载荷为 515 字节私有结构（含 `dims=1920x1440`）。

7. 视频必须全 I 帧（每一帧都是同步帧）。
   - 常规 GOP 下（例如 `-g 15`）3 秒视频只有 7 个关键帧，iOS 编辑实况照片拖动时间轴只显示 7 帧，且只能在这 7 帧里选封面。
   - 全 I 帧（`-g 1`）下 90 帧全部是同步帧，可逐帧拖动、任选封面。
   - 全 I 帧的码率/体积约为常规 GOP 的 3~4 倍。

8. 视频编码 hvc1 和 avc1 均可；音频 AAC 任意声道数/采样率（44.1k/48k/22.05k）。时间轴/采样表保留源时序，不归一化成固定 timescale。

#### QuickTime 旋转矩阵

Apple 使用 3×3 仿射变换矩阵表示视频方向，而非单一角度值：

| 矩阵 | 含义 | 等效旋转 |
|------|------|:---:|
| `[1 0 0 · 0 1 0 · 0 0 1]` | 正常 | 0° |
| `[0 1 0 · -1 0 0 · 0 0 1]` | 正手持拍摄 | 顺时针 90° |
| `[-1 0 0 · 0 -1 0 · 0 0 1]` | 倒置拍摄 | 180° |
| `[0 -1 0 · 1 0 0 · 0 0 1]` | 反手持拍摄 | 顺时针 270° |
| `[1 0 0 · 0 -1 0 · 0 0 1]` | 前置摄像头翻转 (A) | 0°（上下镜像） |
| `[-1 0 0 · 0 1 0 · 0 0 1]` | 前置摄像头翻转 (B) | 180°（上下镜像） |

数值可能以普通整数（-1, 0, 1）或 16.16 定点数（65536=1.0, 16384=0.25）表示。

#### 常见问题

| 问题 | 原因 | 表现 |
|------|------|------|
| 缩略图横向拉伸 | iOS 17.3 之前 Apple 嵌入低分辨率缩略图但保留方向标签 | Windows 将其当作横向图片，导致宽高比错误 |
| 前置摄像头视频旋转异常 | 前置摄像头纵向像素横向存储，方向由旋转矩阵指示 | Windows 不读取 QuickTime 旋转矩阵，视频方向错误 |
| HEIC 方向不一致 | EXIF `Orientation` 与 `QuickTime:Rotation` 标注不同 | 不同软件读取不同标签，显示方向各异 |
| ContentIdentifier 丢失 | 跨平台传输时部分工具不保留 QuickTime 元数据 | 照片与视频配对关系断裂 |

---

### vivo Live Photo（≤X200 系列）

早期 vivo 实况照片采用双文件 + 私有 JSON 尾标的方案。实测 iQOO 12（OriginOS 4）。


#### 文件结构

一张 `.jpg` + 一个 `.mp4`，两者文件名前缀相同，存放在同一目录下。删除 `.mp4` → 失去动态效果。


#### 配对机制

两个文件**各自**在尾部携带一份私有 JSON，vivo 相册通过 `com.android.camera.livephoto` 字段的值进行配对。JSON 中其余字段均为 vivo 相机私有数据（设备信息、拍摄参数等），与实况照片识别无关。

**JPEG 尾部** — 文件末尾，格式为 `vivo{JSON}cameralbum!`：

```
vivo{"com.android.camera.hdr":49426,
     "com.android.camera.joint.fullview.orientation":0,
     "com.android.camera.fisheye":-1,
     "com.android.camera.takenmodel":"iQOO 12",
     "com.android.camera.camerafacing":"0",
     "com.android.camera.livephoto":"1781689481524ba274bd00000000",
     "version":2104,
     ...}
cameralbum!

```
**MP4 尾部** — ISOBMFF `uuid` box（user type = `vivoMediaExtInfo`），同样包含 `vivo{JSON}`：

```
vivo{"com.android.camera.takenmodel":"iQOO 12",
     "com.android.camera.camerafacing":"0",
     "com.android.camera.imageTime":0,
     "com.android.camera.moduleid":"photo",
     "com.android.camera.livephoto":"1781689481524ba274bd00000000",
     "version":2104,
     "com.android.camera.faceInfo":{}}

```
| 字段 | 位置 | 说明 | 与实况相关？ |
|------|------|------|:---:|
| `com.android.camera.livephoto` | JPEG + MP4 | 实况照片配对 ID，两端一致 | ✅ |
| `cameralbum!` | 仅 JPEG | 尾部标记，标识此文件为实况照片 JPEG | ✅ |
| `uuidvivoMediaExtInfo` | 仅 MP4 | ISOBMFF box，标识此文件为实况照片 MP4 | ✅ |
| `com.android.camera.takenmodel` | JPEG + MP4 | 设备型号 | 否 |
| `com.android.camera.camerafacing` | JPEG + MP4 | 摄像头方向 | 否 |
| `com.android.camera.moduleid` | JPEG + MP4 | 拍摄模块（`"photo"`） | 否 |
| `version` | JPEG + MP4 | 格式版本号，因设备而异 | 否 |
| `com.android.camera.imageTime` | 仅 MP4 | 图片时间戳 | 否 |
| `com.android.camera.faceInfo` | 仅 MP4 | 人脸检测数据 | 否 |
| `com.android.camera.hdr` | 仅 JPEG | HDR 模式参数 | 否 |
| `com.android.camera.fisheye` | 仅 JPEG | 鱼眼镜头参数 | 否 |
| `com.android.camera.joint.fullview` | 仅 JPEG | 全屏取景标记 | 否 |
| `com.android.camera.joint.fullview.orientation` | 仅 JPEG | 取景方向 | 否 |
| `com.vivo.xdr.maxGain` | 仅 JPEG | XDR 增益值 | 否 |
| `com.android.camera.watermarkVersion` | 仅 JPEG | 水印版本 | 否 |
| `com.android.camera.project` | 仅 JPEG | 内部项目代号 | 否 |

> 🔴 以下字段仅在社区参考中出现过（iQOO 7），实测 iQOO 12 不存在，是否为固定字段待确认：
> - `com.android.camera.document` — 文档扫描模式
> - `com.android.camera.joint.conshoot` — 连拍标记
> - `com.android.camera.joint.motioncapture` — 🔴 名字含 "motion"，推测与动态拍摄有关，但值为 `0` 且在 iQOO 12 缺失，实况照片功能不依赖它


#### 实用性结论

旧格式跨品牌兼容性极差，已被 X300 系列单文件 V2 取代，不建议基于此格式开发。

## 🔍 检测与识别


### 单文件格式（Google V2 / OPPO O-Live / 小米 Mi-Live / vivo Live）

读取 JPEG 文件前 1 MB，搜索以下特征字符串：

**Google MicroVideo V1**:
- `GCamera:MicroVideo="1"`
- `GCamera:MicroVideoOffset`

**Google MotionPhoto V2**:
- `GCamera:MotionPhoto="1"`
- `Container:Directory`
- `Item:Semantic="MotionPhoto"`

**OPPO O-Live Photo**（兼容 V2，额外检测以下标记）:
- `OpCamera:MotionPhotoOwner="oplus"`
- `xmlns:OpCamera`
- EXIF `UserComment` 以 `oplus_` 开头

**小米 Mi-Live Photo**（就是 Google V2，额外检测私有标签以区分来源）:
- `MiCamera:XMPMeta`（`video_madrid_image`，旧样本为 `MiCamera:VideoLength`）

**vivo Live Photo**（X300 系列：3-item Ultra HDR XMP，检测 VCamera 区分来源）:
- `VCamera:VMotionPhotoVersion`
- `xmlns:VCamera`
- 完整结构见 [vivo X300 章节](#vivo-x300)：hdrgm + Primary/GainMap/MotionPhoto 三段式 + 非空 EXIF UserComment

**三星 Motion Photo**（双轨制：V2 XMP 兼容 Google Photos + 私有 Trailer 供三星相册）:

- Samsung Trailer 中包含 `MotionPhoto_Data` 标签（marker `0x0a30`）及其 data 部分（视频）
- SEF 验证区魔数 `SEFH` / `SEFT` 存在且索引一致
- HEIC 容器中检测 `mpvd` / `sefd` box
- 同时满足 MotionPhoto V2 XMP 字段（`GCamera:MotionPhoto` + `Container:Directory`，供 Google Photos 兼容）

**华为 Moving Photo（HEIC / JPEG）**：
- 文件尾部搜索 `LIVE_` 字节序列 ← 唯一必需判定标记
- 验证 `LIVE_` 值 = 嵌入 MP4 大小 + 20
- 搜索嵌入 MP4：HEIC 搜第二个 `ftyp`（major=`mp42`），JPEG 搜 JPEG EOI 之后的 `ftyp`
- ⚠️ `tmap` brand 和 `com.openharmony.timed_metadata.movingphoto` 轨道均非必需

**荣耀 Moving Photo**（与华为相同协议）：
- 同华为检测逻辑，`Make = "HONOR"` 辅助判断

命中任一标记即可判定为候选。进一步解析视频偏移量/长度，验证数值合理性（>4KB、<文件总大小），即可确认。

> 注意：
> - Google/OPPO 的命名空间 URI 在不同设备/系统版本间存在格式差异（空格、引号、`http` vs `https`），因此直接搜索**短属性名**（如 `GCamera:MotionPhoto`）兼容性最好，不建议精确匹配完整 URI。
> - 华为不使用 XMP，因此 XMP 搜索对其无效。必须使用尾部扫描（`LIVE_`）或盒子结构解析（`ftypmp42` 定位）。

---

### 双文件格式（Apple Live / vivo Live ≤X200）

**Apple — 文件名配对**

扫描目录中扩展名基础部分相同的 `.heic`/`.jpg` 与 `.mov`/`.mp4` 文件，初步配对。

**Apple — ContentIdentifier UUID 匹配**

读取照片和视频的 `ContentIdentifier`，UUID 一致即确认为一对。比文件名配对可靠——文件被重命名不影响配对。

**vivo 旧格式 — JPEG 尾部 JSON**

读取 `.jpg` 文件末尾，搜索 `vivo{"` 特征字符串 → 解析 JSON → 取 `com.android.camera.livephoto` 作为关联 ID → 与同名 `.mp4` 配对。

---

### 可靠性排序

| 优先级 | 方法 | 适用范围 | 可靠性 |
|--------|------|----------|:---:|
| 1 | XMP 协议标识字段 | Google V1/V2 / 小米 Mi-Live / vivo Live / OPPO O-Live | 最高 |
| 2 | `Container:Directory` 结构 | Google V2 / OPPO O-Live / vivo Live / 小米 Mi-Live | 高 |
| 3 | 视频偏移量有效且合理 | 所有单文件格式 | 高 |
| 4 | ContentIdentifier UUID 匹配 | Apple Live | 高 |
| 5 | Samsung Trailer `EmbeddedVideoType` | 三星 Motion | 高 |
| 6 | 文件尾 `LIVE_` 标记 | 华为 Moving（HEIC+JPEG）/ 荣耀 Moving | 最高（官方确认） |
| 7 | `LIVE_` + 嵌入 MP4 存在 | 华为 Moving / 荣耀 Moving | 高 |
| 8 | 文件名配对 | Apple Live / vivo Live（≤X200） | 中 |
| 9 | vivo JPEG 尾部 JSON | vivo Live（≤X200） | 中 |
| 10 | 日期 + GPS 兜底匹配 | 通用 | 低 |

---

## 📊 协议对比速查


### 基本信息

| | Google MicroVideo V1 | Google MotionPhoto V2 | 小米 Mi-Live Photo | OPPO O-Live Photo | vivo Live Photo | 华为 Moving Photo（HEIC） | 华为 Moving Photo（JPEG） | 荣耀 Moving Photo | 三星 Motion Photo | Apple Live Photo | vivo Live Photo（≤X200） |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 制定者 | Google | Google | 小米 | OPPO | vivo | 华为 | 华为 | 荣耀 | 三星 | Apple | vivo |
| 状态 | 已弃用 | 现行标准 | 现行 | 现行 | 现行 | 现行 | 现行 | 现行 | 现行 | 现行 | 现行 |
| 打包方式 | 单文件 | 单文件 | 单文件 | 单文件 | 单文件 | 单文件 | 单文件 | 单文件 | 单文件 | 双文件 | 双文件 |
| 文件后缀 | `.jpg` | `.jpg` / `.heic` / `.avif` | `.jpg` | `.jpg` | `.jpg` | `.heic` | `.jpg` | `.jpg` | `.jpg` / `.heic` | `.heic` + `.mov` | `.jpg` + `.mp4` |
| 图片格式 | JPEG | JPEG / HEIC / AVIF | JPEG | JPEG | JPEG | HEVC (HEIC 容器内) | JPEG | JPEG | JPEG / HEIC | HEIC | JPEG |
| 视频格式 | MP4 | MP4 / MOV | MP4 | MP4 | MP4 | MP4 (嵌入 HEIC 容器) | MP4 (嵌入 JPEG 中段) | MP4 (嵌入 JPEG 中段) | MP4 | MOV (QuickTime) | 独立 MP4 |
| 元数据方式 | XMP | XMP | XMP | XMP | XMP | 无 XMP，私有二进制 | 无 XMP，私有二进制 | 无 XMP，私有二进制 | Samsung Trailer + XMP (双轨) | QuickTime | 私有 JSON 尾标 |

---

### XMP 命名空间使用

| 命名空间 | URI | Google V1 | Google V2 | OPPO O-Live | 小米 Mi-Live | vivo Live |
|------|------|:---:|:---:|:---:|:---:|:---:|
| `GCamera` | `http://ns.google.com/photos/1.0/camera/` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `Container` | `http://ns.google.com/photos/1.0/container/` | — | ✅ | ✅ | ✅ | ✅ |
| `Item` | `http://ns.google.com/photos/1.0/container/item/` | — | ✅ | ✅ | ✅ | ✅ |
| `OpCamera` | `http://ns.oplus.com/photos/1.0/camera/` | — | — | ✅ | — | — |
| `MiCamera` | `http://ns.xiaomi.com/photos/1.0/camera/` | — | — | — | ✅ | — |
| `VCamera` | `http://ns.vivo.com/photos/1.0/camera/` | — | — | — | — | ✅ |
| `hdrgm` | `http://ns.adobe.com/hdr-gain-map/1.0/` | — | — | — | — | ✅ |

> Apple 不使用 XMP。元数据存储在 QuickTime 容器内。

---

### 视频定位方式

| 协议 | 视频定位 | 封面时间戳字段 |
|------|----------|----------------|
| Google V1 | `MicroVideoOffset`（从 EOF 前推） | `MicroVideoPresentationTimestampUs`（微秒） |
| Google V2 | `Item:Length`（Container:Directory 第 2 个 Item） | `MotionPhotoPresentationTimestampUs`（微秒） |
| 小米 Mi-Live | 同 Google V2 | 同 Google V2 |
| OPPO O-Live | `Item:Length` 为主，`OpCamera:VideoLength` 为辅 | `MotionPhotoPresentationTimestampUs` + `MotionPhotoPrimaryPresentationTimestampUs`（微秒） |
| vivo Live | `Item:Length`（Container:Directory 第 3 个 Item，MotionPhoto） | `MotionPhotoPresentationTimestampUs`（微秒，真实样本非 0） |
| 华为 Moving（HEIC） | 嵌入 MP4 ftyp box（搜索第二个 `ftyp`，major=`mp42`） | `com.openharmony.covertime`（新相册，毫秒）+ `v6_fXX`（旧相册，帧序号） |
| 华为 Moving（JPEG） | 嵌入 MP4 ftyp box（搜索 `ftypmp42` 字节序列） | 同 HEIC |
| 荣耀 Moving | 同华为 JPEG | 同华为 JPEG |
| 三星 Motion | XMP `Container:Directory` + Samsung Trailer SEF 索引 `0x0a30`（三星相册） | 同 Google V2 `MotionPhotoPresentationTimestampUs`（微秒） |
| Apple Live | 视频为独立文件 | Metadata 轨 `StillImageTime=-1` 所在轨的 `TrackDuration` |
| vivo Live（≤X200） | 视频为独立 MP4 文件 | — |

---

### 厂商特有标记

| 厂商 | 标记位置 | 标记内容 |
|------|----------|----------|
| Google | XMP `GCamera` | `MotionPhoto="1"` / `MicroVideo="1"` |
| 小米 | EXIF `Make` + `Model` | `"Xiaomi"` + 型号 |
| 小米 | EXIF `0x889e` | JSON（`buildDevice` + `livephotoInfo`） |
| OPPO | XMP `OpCamera` | `MotionPhotoOwner="oplus"` |
| OPPO | EXIF `UserComment` | `oplus_` 前缀 |
| vivo | XMP `VCamera` | `VMotionPhotoVersion` + `VMotionPhotoSource` |
| 华为 | 文件尾（60 B） | `v6_fXX` = 封面帧序号（旧版相册）、`PPP:QQQQ` = 帧号:总帧数（只读）、`LIVE_` = MP4 字节数 + 20 |
| 华为 | MP4 udta | `com.openharmony.covertime` = 封面帧毫秒时间戳（**新版相册**） |
| 华为 | MP4 udta | `©too` = `"Openharmony6.1"`（编码器标识） |
| 华为 | 静态图片 | 封面帧本体——改封面即替换此静态图片，视频保留完整 |
| 荣耀 | 文件尾（60 B） | 同华为尾部 + `uuidextend_type_matrix` JSON（防抖数据，非实况） |
| 荣耀 | 文件尾（moov 之后） | `uuidextend_type_matrix` + 逐帧防抖 JSON（荣耀私有扩展，非实况） |
| 三星 | Samsung Trailer（JPEG） | `MotionPhoto_Data`（marker `0x0a30`）+ `SEFH`/`SEFT` 魔数 + 7 Tag 索引 |
| 三星 | ISOBMFF box（HEIC） | `mpvd` (Motion Photo Video Data) + `sefd` (Samsung Extended File Data) |
| Apple | QuickTime `ContentIdentifier` | 共享 UUID |

---

## 🔗 相关资源

| 资源 | 说明 |
|------|------|
| [GitHub 仓库](https://github.com/lengxiqwq/live-photo-box) | 项目源码 · 提交记录 · Issues |
| [项目总览](docs/项目总览.md) | 项目结构、技术栈与构建文档 |
| [更新日志](changelogs/CHANGELOG.zh-CN.md) | 各版本发布说明 |
| [Release 下载](https://github.com/lengxiqwq/live-photo-box/releases) | 安装版 / 便携版 / CLI |
| [问题反馈](https://github.com/lengxiqwq/live-photo-box/issues) | Bug 报告 / 功能建议 |

---

<p align="center">
  <sub>Made with ❤️ by <a href="https://github.com/lengxiqwq">LengxiQwQ</a></sub>
</p>
