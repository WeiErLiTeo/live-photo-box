# 外部工具定制编译指南

> 将项目依赖的外部二进制工具从官方预编译包中剥离，只保留实际所需功能，通过静态链接 + UPX 压缩实现极致瘦身。

**最后编译**: 2026-07-22（添加 GIF 编码器/palettegen/paletteuse 滤镜）
**编译环境**: MSYS2 + MinGW-w64 (GCC 16.1.0)

---

## 目录

- [0. 编译环境搭建](#0-编译环境搭建)
- [第一部分：FFmpeg](#第一部分ffmpeg)
  - [1. 功能清单](#1-ffmpeg-功能清单)
  - [2. 编译流程](#2-ffmpeg-编译流程)
  - [3. 故障排查](#3-ffmpeg-故障排查)
- [第二部分：libheif 工具](#第二部分libheif-工具)
  - [4. 功能清单](#4-libheif-功能清单)
  - [5. 编译流程](#5-libheif-编译流程)
  - [6. 故障排查](#6-libheif-故障排查)
- [附录](#附录)
  - [A. UPX 压缩](#a-upx-压缩)
  - [B. 部署到项目](#b-部署到项目)
  - [C. 工具目录总览](#c-工具目录总览)

---

## 0. 编译环境搭建

所有工具均在 **MSYS2 + MinGW-w64** 环境下编译。

### 0.1 安装 MSYS2

从 https://github.com/msys2/msys2-installer/releases 下载最新安装器，安装到 `C:\msys64`。

或使用命令行静默安装：

```powershell
curl -L -o msys2-installer.exe "https://github.com/msys2/msys2-installer/releases/download/<date>/msys2-x86_64-<date>.exe"
.\msys2-installer.exe in --confirm-command --accept-messages --root C:/msys64
```

### 0.2 安装共用编译工具

打开 **MINGW64** shell（`C:\msys64\mingw64.exe`），运行：

```bash
# 首次更新系统
pacman -Syu

# 安装共用工具链
pacman -S --needed --noconfirm \
    mingw-w64-x86_64-toolchain \
    mingw-w64-x86_64-cmake \
    mingw-w64-x86_64-ninja \
    mingw-w64-x86_64-nasm \
    mingw-w64-x86_64-upx \
    git make diffutils pkgconf
```

**依赖说明**：

| 包名 | 用途 |
|------|------|
| `mingw-w64-x86_64-toolchain` | GCC 16.1.0 + binutils + WinSDK 头文件 |
| `mingw-w64-x86_64-cmake` | CMake 构建系统（libheif 需要） |
| `mingw-w64-x86_64-ninja` | Ninja 构建工具（libheif 需要） |
| `mingw-w64-x86_64-nasm` | x86 汇编器（加速编解码器） |
| `mingw-w64-x86_64-upx` | UPX 压缩工具 |
| `git` | 拉取源码 |

---

# 第一部分：FFmpeg

## 1. FFmpeg 功能清单

> 将 gyan.dev full_build 从 **97 MB** 瘦身至 **5.8 MB**（缩减 94%），精确保留项目实际所需的全部功能。

**当前版本**: n8.0.1
**编译日期**: 2026-07-03

以下按 C# 服务文件逐一列出每个 ffmpeg 参数及其对应的编译组件。

### 1.1 ThumbnailService.cs — 视频缩略图提取

**文件**: `LivePhotoBox/Services/ThumbnailService.cs`

```bash
ffmpeg [-hwaccel cuda|qsv|d3d11va|vaapi] -i "{video}" -vframes 1 \
  -vf "scale=80:-1:force_original_aspect_ratio=decrease" \
  -q:v 2 "{thumb.jpg}" -y -loglevel error
```

| 参数 | 功能 | 编译要求 |
|------|------|---------|
| `-hwaccel cuda/qsv/d3d11va/vaapi` | GPU 硬件解码加速 | 对应 hwaccel |
| `scale=...` | 缩放到 80px 宽 | `--enable-filter=scale` |
| `-q:v 2` | JPEG 质量参数 | `--enable-encoder=mjpeg` |
| `.jpg` 输出 | 输出为 JPEG | `--enable-muxer=image2` |

### 1.2 VideoTranscodeService.cs — 视频转码 & Remux

**文件**: `LivePhotoBox.Core/Services/VideoTranscodeService.cs`

#### Remux 模式（流拷贝）

```bash
ffmpeg -y -i "{input}" -c copy -map 0:V:0 -map 0:a:0? \
  -map_metadata 0 [-movflags +faststart] "{output}"
```

#### 转码模式

```bash
ffmpeg -apply_cropping 0 -y -i "{input}" \
  -map 0:V:0 -map 0:a:0? -map_metadata 0 \
  -threads {N} -vf "{filter}" -pix_fmt yuv420p \
  -c:v {encoder} {params} {audio_args} \
  -movflags +faststart "{output}"
```

**项目支持的 8 种视频编码器**：

| 编码器 | 类型 | 编译要求 |
|--------|------|---------|
| `libx264` | CPU H.264 | `--enable-libx264` |
| `libx265` | CPU HEVC | `--enable-libx265` |
| `h264_amf` / `hevc_amf` | AMD GPU | `--enable-amf` |
| `h264_nvenc` / `hevc_nvenc` | NVIDIA GPU | `--enable-nvenc` + ffnvcodec |
| `h264_qsv` / `hevc_qsv` | Intel GPU | `--enable-libvpl` |

### 1.3 LivePhotoRepairService.cs — 视频修复

**文件**: `LivePhotoBox.Core/Services/LivePhotoRepairService.cs`

```bash
ffmpeg -apply_cropping 0 -y -i "{source}" \
  -map 0:v:0 -map 0:a:0? -map_metadata 0 \
  -threads {N} -vf "{transform_filter},setsar=1" \
  -c:v {encoder} -pix_fmt yuv420p \
  -fflags +genpts -c:a aac -b:a 192k \
  -movflags +faststart [-tag:v hvc1] "{output}"
```

**视频变换滤镜**：`transpose`（旋转）、`hflip`、`vflip` → `--enable-filter`

### 1.4 完整组件汇总

#### 编码器 (10 个)

```
libx264  h264_amf  h264_nvenc  h264_qsv
libx265  hevc_amf  hevc_nvenc  hevc_qsv
mjpeg    aac       gif
```

#### 解码器 (15 个)

```
h264  hevc  mjpeg  mpeg1video  mpeg2video  mpeg4
aac   mp3   mp2    pcm_s16le   pcm_s24le   pcm_f32le  pcm_s16be
```

#### 滤镜 (18 个)

```
scale  transpose  hflip  vflip  crop  null  anull
format  setsar  fps  setpts  settb  trim  atrim
aformat  aresample  pad  buffer  buffersink
palettegen  paletteuse  split
```

#### 封装器

```
Muxer:   mp4  mov  mp3  image2  null  mjpeg  gif
Demuxer: mov  mp4  mpegts  matroska  avi  image2  aac  mp3  wav  mjpeg
```

#### 其他

```
硬件加速:  cuda  vaapi  dxva2  qsv  d3d11va  d3d12va  amf
BSF:       h264_mp4toannexb  hevc_mp4toannexb  aac_adtstoasc  extract_extradata  filter_units
Protocol:  file  pipe
```

### 1.5 EditViewModel.cs — GIF 动图导出

**文件**: `LivePhotoBox/ViewModels/EditViewModel.cs` + `LivePhotoBox.Core/Services/VideoTranscodeService.cs`

```bash
ffmpeg -y -i "{video}" \
  -vf "fps={fps},scale={w}:{h}:flags=lanczos,split[a][b];[a]palettegen=max_colors=256:stats_mode=diff[p];[b][p]paletteuse=dither=bayer:bayer_scale=5" \
  -loop {loop} "{output.gif}"
```

| 参数 | 功能 | 编译要求 |
|------|------|---------|
| `fps={n}` | 控制输出帧率 | `--enable-filter=fps`（已有） |
| `scale=w:h:flags=lanczos` | Lanczos 缩放到指定尺寸 | `--enable-filter=scale`（已有） |
| `split[a][b]` | 分流为两个输出 | `--enable-filter=split`（**新增**） |
| `palettegen=max_colors=256:stats_mode=diff` | 基于帧间差异生成最优 256 色 GIF 调色板 | `--enable-filter=palettegen`（**新增**） |
| `paletteuse=dither=bayer:bayer_scale=5` | Bayer 有序抖动应用调色板 | `--enable-filter=paletteuse`（**新增**） |
| `-loop {n}` | 循环次数（0=无限） | `--enable-muxer=gif`（**新增**） |
| `.gif` 输出 | GIF 编码 + 容器写入 | `--enable-encoder=gif` + `--enable-muxer=gif`（**新增**） |

> **设计说明**: 滤镜顺序刻意设为 scale 在 fps 之前——先缩小像素再降帧率，减少中间帧的内存占用。
> palettegen 的 `stats_mode=diff` 只分析帧间变化区域的色彩分布，避免静态背景浪费调色板条目。
> paletteuse 的 `dither=bayer` 采用 Bayer 有序抖动（非 Floyd-Steinberg 误差扩散），消除 GIF 帧间闪烁。

---

## 2. FFmpeg 编译流程

以下命令在 **MINGW64** shell 中执行。

### 2.1 安装 FFmpeg 编译依赖

```bash
pacman -S --needed --noconfirm \
    mingw-w64-x86_64-libx264 \
    mingw-w64-x86_64-x264 \
    mingw-w64-x86_64-x265 \
    mingw-w64-x86_64-libvpl \
    mingw-w64-x86_64-amf-headers \
    mingw-w64-x86_64-ffnvcodec-headers
```

| 包名 | 用途 |
|------|------|
| `mingw-w64-x86_64-libx264` | H.264 编码库（含头文件和静态库） |
| `mingw-w64-x86_64-x265` | HEVC 编码库（含头文件和静态库） |
| `mingw-w64-x86_64-libvpl` | Intel oneVPL（QSV 硬件编码，需 `-lstdc++`） |
| `mingw-w64-x86_64-amf-headers` | AMD AMF SDK 头文件 |
| `mingw-w64-x86_64-ffnvcodec-headers` | NVIDIA NVENC SDK 头文件 |

### 2.2 克隆 & 编译

```bash
cd /c/Users/LengxiQwQ/Downloads
git clone --depth 1 --branch n8.0.1 https://git.ffmpeg.org/ffmpeg.git ffmpeg-src
cd ffmpeg-src

./configure \
    --prefix=/c/Users/LengxiQwQ/Downloads/ffmpeg-out \
    --target-os=mingw32 \
    --arch=x86_64 \
    --enable-gpl \
    --enable-version3 \
    --enable-static \
    --disable-shared \
    --disable-debug \
    --disable-doc \
    --enable-stripping \
    --enable-ffmpeg \
    --disable-ffplay \
    --disable-ffprobe \
    --disable-avdevice \
    --disable-network \
    --disable-everything \
    --enable-libx264 \
    --enable-libx265 \
    --enable-amf \
    --enable-libvpl \
    --enable-nvenc \
    --enable-d3d11va \
    --enable-dxva2 \
    --enable-pthreads \
    --disable-w32threads \
    --pkg-config-flags="--static" \
    --extra-libs="-lstdc++" \
    --enable-encoder=libx264,libx265,aac,mjpeg,gif \
    --enable-encoder=h264_amf,hevc_amf,h264_nvenc,hevc_nvenc,h264_qsv,hevc_qsv \
    --enable-decoder=h264,hevc,mjpeg,mpeg1video,mpeg2video,mpeg4 \
    --enable-decoder=aac,mp3,mp2,pcm_s16le,pcm_s24le,pcm_f32le,pcm_s16be \
    --enable-muxer=mp4,mov,mp3,image2,null,mjpeg,gif \
    --enable-demuxer=mov,mp4,mpegts,matroska,avi,image2,aac,mp3,wav,mjpeg \
    --enable-parser=h264,hevc,mpegaudio,aac,mjpeg,mpeg4video,mpegvideo \
    --enable-bsf=h264_mp4toannexb,hevc_mp4toannexb,aac_adtstoasc,extract_extradata,filter_units \
    --enable-filter=scale,transpose,hflip,vflip,crop,null,anull,format,setsar,fps,setpts,settb \
    --enable-filter=trim,atrim,aformat,aresample,pad,buffer,buffersink,palettegen,paletteuse,split \
    --enable-protocol=file,pipe \
    --enable-hwaccel=h264_d3d11va,h264_dxva2,hevc_d3d11va,hevc_dxva2 \
    --extra-cflags="-O2" \
    --extra-ldflags="-s -static -static-libgcc -static-libstdc++"

    # 关键步骤：移除 ffmpeg configure 多余加入的 -lgcc_s。MinGW 的 libgcc_s.a
    # 实际上是 DLL 导入桩，强行链接会导致 ffmpeg.exe 依赖 libgcc_s_seh-1.dll。
    # -static-libgcc 已让 GCC 内部使用 libgcc.a + libgcc_eh.a，
    # 所以 EXTRALIBS 里的 -lgcc_s 是多余且有害的，必须移除。
    sed -i 's/-lgcc_s//g' ffbuild/config.mak

make -j20
```

**Configure 核心参数说明**：

| 参数 | 说明 |
|------|------|
| `--disable-everything` | 先关闭一切，再精确启用所需组件 |
| `--enable-static --disable-shared` | 静态链接，产出单文件 exe |
| `--pkg-config-flags="--static"` | pkg-config 返回静态库链接参数 |
| `--extra-libs="-lstdc++"` | 链接 C++ 标准库（libvpl 需要） |
| `--extra-ldflags="-s -static -static-libgcc -static-libstdc++"` | 链接时 strip + 完全静态链接（包括 GCC 运行时） |
| `sed -i 's/-lgcc_s//g' ffbuild/config.mak` | **关键**：移除 configure 误加的 `-lgcc_s` 动态导入桩引用 |

### 2.3 验证

```bash
# 验证零外部 DLL 依赖（应只有系统 DLL）
objdump -p ffmpeg.exe | grep "DLL Name"

# 验证全部编码器
./ffmpeg.exe -hide_banner -encoders 2>&1 | grep -E "libx264|libx265|amf|nvenc|qsv|mjpeg|gif"

# 验证全部滤镜
./ffmpeg.exe -hide_banner -filters 2>&1 | grep -E "scale|transpose|hflip|vflip|crop|setsar|palette"

# 验证全部封装器
./ffmpeg.exe -hide_banner -muxers 2>&1 | grep -E "mp4|mov|gif"

# 验证硬件加速
./ffmpeg.exe -hide_banner -hwaccels 2>&1
```

### 2.4 版本升级

```bash
cd /c/Users/LengxiQwQ/Downloads/ffmpeg-src
git fetch --tags
git tag -l 'n*' | tail -20          # 查看可用版本
git checkout nX.Y.Z
make distclean
	# 重新运行 ./configure ...（参数同上）
	sed -i 's/-lgcc_s//g' ffbuild/config.mak
	make -j20
```

> **已知风险**：大版本升级可能改变编码器名或移除选项。升级后应做冒烟测试。

---

## 3. FFmpeg 故障排查

### libvpl 检测失败

```
ERROR: libvpl >= 2.6 not found
```

原因：libvpl 是 C++ 库，configure 测试链接时缺少 C++ 标准库。
解决：添加 `--extra-libs="-lstdc++"` 到 configure 命令。

### x264 检测失败

```
ERROR: x264 not found using pkg-config
```

原因：配置静态链接时 pkg-config 默认返回动态库参数。
解决：添加 `--pkg-config-flags="--static"`。


### ffmpeg.exe 依赖 libgcc_s_seh-1.dll（STATUS_DLL_NOT_FOUND / 0xC0000135）

```
错误码: -1073741515 (0xC0000135 = STATUS_DLL_NOT_FOUND)
```

**症状**：ffmpeg.exe 在命令行（Git Bash、Cmd）中可以正常运行，但被 C# 程序
通过 Process.Start 调用时启动失败，程序报告 "错误编码 -1073741515"。
硬件检测页面显示 "FFmpeg — 不可用"、"版本: 未知"、"错误: Exit code: -1073741515"。

**原因**：MinGW GCC 16.1.0 的 `libgcc_s.a` 是一个 **DLL 导入桩**（仅 80 KB），
不是真正的静态库。即便 configure 加了 `--extra-ldflags="-static"`，
ffmpeg 的 configure 脚本仍然在 `EXTRALIBS-avcodec` 中加入了 `-lgcc_s`，
导致链接器找到导入桩，使 ffmpeg.exe 在运行时需要 `libgcc_s_seh-1.dll`。
同样 `libwinpthread-1.dll` 也会出现同样问题。
Git Bash 中能运行是因为 `/mingw64/bin/` 在 PATH 中带有这两个 DLL，
而普通 Windows 进程没有。

**解决方案**（二选一）：

**方案 A：在 configure 后移除 -lgcc_s（推荐）**

在 `./configure` 之后、`make` 之前执行：
```bash
sed -i 's/-lgcc_s//g' ffbuild/config.mak
```
同时 `--extra-ldflags` 必须包含 `-static-libgcc -static-libstdc++`：
```bash
--extra-ldflags="-s -static -static-libgcc -static-libstdc++"
```
这样 GCC 内部会使用 `libgcc.a` + `libgcc_eh.a`（真正的静态代码，3 MB），
不需要外部的 `libgcc_s_seh-1.dll`。

**方案 B：把运行时 DLL 放到 Tools 目录（不推荐）**
将 `libgcc_s_seh-1.dll` 和 `libwinpthread-1.dll` 复制到 ffmpeg.exe 旁边：
```bash
cp /mingw64/bin/libgcc_s_seh-1.dll /path/to/Tools/
cp /mingw64/bin/libwinpthread-1.dll /path/to/Tools/
```
缺点：每次编译都要带这两个 DLL，且 C# 项目中的 MSIX 打包可能会报 DEP1000 错误。

**验证方法**：
```bash
# 应该只有 Windows 系统 DLL，没有 /mingw64/bin/ 下的文件
objdump -p ffmpeg.exe | grep "DLL Name"
# 预期输出：
#   DLL Name: ADVAPI32.dll
#   DLL Name: bcrypt.dll
#   DLL Name: KERNEL32.dll
#   DLL Name: msvcrt.dll
#   DLL Name: ole32.dll
#   DLL Name: SHELL32.dll
#   DLL Name: USER32.dll
```

### 编译产物过大

- 检查 configure 输出中的 `External libraries` 列表
- 确保 `--disable-everything` 后没有连带多余组件
- UPX 压缩是最后的瘦身手段

### C# 项目 MSIX 打包报 DEP1000（文件找不到）

```
错误 DEP1000: 无法将文件 "...\\Tools\\xxx.dll" 复制到布局
```

**症状**：删除 Tools 目录中的某些文件后（如旧的备份 exe 或运行时 DLL），
重新编译 C# 项目时提示不能复制已不存在的文件。

**原因**：MSBuild 会将 `Content Include="Tools\**\*"` 匹配到的文件列表
缓存到 `obj` 目录中。文件从磁盘删除后，缓存仍然引用它，
导致 MSIX 打包阶段找不到该文件而报错。

**解决**：清除 MSBuild 缓存，让项目重新扫描 Tools 目录：
```bash
# 删除 obj 和 bin 目录（编译时会自动重建）
rm -rf "LivePhotoBox/obj" "LivePhotoBox/bin"
```

> **预防措施**：在 csproj 的 Content Include 中加入 `Exclude` 属性，
> 让备份文件等临时文件不进入 MSIX 追踪列表：
> ```xml
> <Content Include="Tools\**\*" Exclude="Tools\**\*backup*;**\*backup*\**\*;Tools\**\*.bak;Tools\**\*.tmp;Tools\**\*.old" />
> ```

---

# 第二部分：libheif 工具

## 4. libheif 功能清单

> 从源码编译 libheif 官方示例工具（heif-enc / heif-dec），仅启用 HEVC（x265）编解码，静态链接所有依赖，零外部 DLL。

**当前版本**: v1.23.1
**编译日期**: 2026-07-17

### 4.1 项目中的使用场景

| 文件 | 调用的工具 | 功能 |
|------|-----------|------|
| `KeyPhotoViewModel.cs:1101` | `heif-enc.exe` | JPEG 帧 → HEIC，"Save for Apple" 导出 |
| `HeicConverterService.cs` | *(Magick.NET，可由 heif-dec 替代)* | HEIC → JPEG 全尺寸解码 |

### 4.2 heif-enc.exe — JPEG → HEIC 编码器

支持的输入格式：JPEG（不含 PNG/TIFF/WebP，已裁剪）
支持的编码器：仅 x265（HEVC）

```bash
# 基本用法
heif-enc -q 90 -o output.heic input.jpg

# 常用参数
-q <1-100>    质量（默认 50）
-o <file>     输出文件路径
--hevc        使用 HEVC 编码（默认）
--lossless    无损模式
```

**C# 调用示例**（来自 `KeyPhotoViewModel.cs`）：
```csharp
string heifArgs = $"-o \"{tempHeicPath}\" -q 90 \"{enrichedJpegPath}\"";
var psi = new ProcessStartInfo
{
    FileName = heifEncPath,
    Arguments = heifArgs,
    UseShellExecute = false,
    CreateNoWindow = true,
    RedirectStandardError = true
};
```

### 4.3 heif-dec.exe — HEIC/HEIF → JPEG 解码器

```bash
# HEIC → JPEG
heif-dec input.heic -o output.jpg -q 95

# 常用参数
-q <1-100>    输出 JPEG 质量
-o <file>     输出文件路径（支持 jpg/png/tif/webp）
--with-exif   额外导出 EXIF 到 .exif 文件
--quiet       静默模式
```

### 4.4 与 Magick.NET 的对比

| 维度 | Magick.NET (NuGet) | heif-dec.exe (本编译) |
|------|-------------------|----------------------|
| 部署方式 | NuGet 包 + 原生 DLL | 单个 3.0 MB .exe |
| HEIC 解码 | ✅ | ✅ |
| 色彩空间 | AutoOrient + sRGB | 内置支持 |
| EXIF 保留 | 需 ExifTool 补 | ✅ 自动保留 |
| 启动开销 | 进程内 | Process.Start |
| 依赖 | Magick.Native (~30 MB) | 零外部 DLL |

---

## 5. libheif 编译流程

### 5.1 设计原则

MSYS2 预编译的 `mingw-w64-x86_64-libheif` 包虽然提供了 `heif-enc.exe` / `heif-dec.exe`，但硬链接了全部编解码器（AV1 ×4、VVC ×2、JPEG2000 ×2……），产生 **64 MB 的 DLL 依赖**（33 个文件）。

本编译方案采用以下策略实现最小化：

1. **仅启用 HEVC**：x265 编码 + libde265 解码，禁用所有其他编解码器
2. **仅保留 JPEG 图像 I/O**：heif-enc 只需读 JPEG，heif-dec 只需写 JPEG
3. **全静态链接**：x265、libde265、libjpeg 全部编译进 .exe
4. **UPX 极限压缩**：27 MB → 3 MB

### 5.2 安装 libheif 编译依赖

```bash
# libheif 编译所需的静态库和头文件
pacman -S --needed --noconfirm \
    mingw-w64-x86_64-x265 \
    mingw-w64-x86_64-libde265
```

> **注意**：`mingw-w64-x86_64-libde265` 需要额外处理。MSYS2 的 libde265 头文件默认标记函数为 `__declspec(dllimport)`，静态链接时必须定义 `LIBDE265_STATIC_BUILD` 宏（见下文 cmake 配置）。

### 5.3 克隆 & 编译

```bash
cd /c/Users/LengxiQwQ/Downloads

# 克隆 libheif 源码（浅克隆，节省时间）
git clone --depth 1 --branch v1.23.1 https://github.com/strukturag/libheif.git libheif-src
cd libheif-src

# 配置：仅启用 x265 + libde265，禁用所有其他编解码器
cmake -B build -G "Ninja" \
    -DCMAKE_BUILD_TYPE=Release \
    -DBUILD_SHARED_LIBS=OFF \
    -DWITH_EXAMPLES=ON \
    -DENABLE_PLUGIN_LOADING=OFF \
    -DWITH_LIBDE265=ON \
    -DWITH_X265=ON \
    -DWITH_AOM_DECODER=OFF -DWITH_AOM_ENCODER=OFF \
    -DWITH_DAV1D=OFF -DWITH_RAV1E=OFF \
    -DWITH_OPENH264=OFF -DWITH_OpenH264_DECODER=OFF \
    -DWITH_KVAZAAR=OFF -DWITH_OPENJPH_ENCODER=OFF \
    -DWITH_SvtEnc=OFF -DWITH_X264=OFF \
    -DWITH_UNCOMPRESSED_CODEC=OFF \
    -DWITH_JPEG_DECODER=OFF -DWITH_JPEG_ENCODER=OFF \
    -DWITH_HEADER_COMPRESSION=OFF \
    -DWITH_GDK_PIXBUF=OFF \
    -DWITH_LIBSHARPYUV=OFF \
    -DWITH_WEBP=OFF -DWITH_WebP=OFF \
    -DWEBPMUX_LIBRARY="" -DWEBPMUX_INCLUDE_DIR="" \
    -DWEBP_LIBRARY="" -DWEBP_INCLUDE_DIR="" \
    -DWEBPMUX_FOUND=FALSE -DWebP_FOUND=FALSE \
    -DJPEG_LIBRARY=/c/msys64/mingw64/lib/libjpeg.a \
    -DJPEG_LIBRARY_RELEASE=/c/msys64/mingw64/lib/libjpeg.a \
    -DJPEG_INCLUDE_DIR=/c/msys64/mingw64/include \
    -DLIBDE265_LIBRARY=/c/msys64/mingw64/lib/libde265.a \
    -DLIBDE265_INCLUDE_DIR=/c/msys64/mingw64/include \
    -DX265_LIBRARY=/c/msys64/mingw64/lib/libx265.a \
    -DX265_INCLUDE_DIR=/c/msys64/mingw64/include \
    -DCMAKE_C_FLAGS="-static -O2 -DLIBDE265_STATIC_BUILD" \
    -DCMAKE_CXX_FLAGS="-static -O2 -DLIBDE265_STATIC_BUILD" \
    -DCMAKE_EXE_LINKER_FLAGS="-static -s" \
    -DPKG_CONFIG_EXECUTABLE=/c/msys64/mingw64/bin/pkg-config.exe

# 编译（仅构建需要的两个目标）
ninja -j20 heif-enc heif-dec
```

**关键 cmake 参数说明**：

| 参数 | 说明 |
|------|------|
| `-DWITH_X265=ON` | 仅启用 x265 HEVC 编码器 |
| `-DWITH_LIBDE265=ON` | 仅启用 libde265 HEVC 解码器 |
| `-DWITH_*_DECODER/ENCODER=OFF` | 禁用 AV1、VVC、JPEG2000 等所有其他编解码器 |
| `-DWITH_WEBP=OFF` + 空 WEBP 变量 | 禁用 WebP 支持（避免引入 libwebp/libwebpmux DLL） |
| `-DJEG_LIBRARY=...libjpeg.a` | 强制使用静态 JPEG 库 |
| `-DLIBDE265_STATIC_BUILD` | **必需**：防止 libde265 头文件产生 `__imp_` DLL 导入符号 |
| `-DCMAKE_EXE_LINKER_FLAGS="-static -s"` | 静态链接 + 去除符号 |

### 5.4 配置输出解读

编译成功后，cmake 输出应显示：

```
=== Summary of compiled codecs ===
libde265 HEVC decoder                 : + built-in
x265 HEVC encoder                     : + built-in
(所有其他编解码器                      : - disabled)

=== Supported formats ===
HEIC            YES        YES
JPEG            NO         NO         ← 内部 JPEG 编解码禁用（不影响 JPEG 文件读写）

=== Active input formats for heif-enc ===
JPEG: active                          ← 通过 libjpeg 读写 JPEG 文件
PNG:  ------ (libpng not found)
TIFF: ------ (libtiff not found)
```

预期结果：
- `heif-enc.exe`：27 MB（UPX 后 3.1 MB）
- `heif-dec.exe`：26 MB（UPX 后 3.0 MB）
- DLL 依赖：仅 `KERNEL32.dll`、`ADVAPI32.dll`、`msvcrt.dll`（系统 DLL）

### 5.5 验证

```bash
# 验证静态链接（应只有系统 DLL）
objdump -p examples/heif-enc.exe | grep "DLL Name"
# 预期输出：
#   DLL Name: ADVAPI32.dll
#   DLL Name: KERNEL32.dll
#   DLL Name: msvcrt.dll

# 功能测试：JPEG → HEIC
./examples/heif-enc.exe -q 90 -o test.heic /path/to/test.jpg

# 功能测试：HEIC → JPEG
./examples/heif-dec.exe test.heic -o test.jpg -q 95

# 验证 HEIC 格式正确性
heif-info.exe test.heic
# 预期：MIME type: image/heic, main brand: heic
```

### 5.6 版本升级

```bash
cd /c/Users/LengxiQwQ/Downloads/libheif-src
git fetch --tags
git tag -l | tail -10
git checkout vX.Y.Z
rm -rf build
# 重新运行 cmake 和 ninja（参数同上）
```

---

## 6. libheif 故障排查

### libde265 静态链接报 `undefined reference to __imp_de265_*`

原因：MSYS2 的 libde265 头文件默认标记为 `__declspec(dllimport)`，静态链接时产生未定义的导入符号。

解决：在 `CMAKE_C_FLAGS` 和 `CMAKE_CXX_FLAGS` 中加入 `-DLIBDE265_STATIC_BUILD`。

### JPEG 仍然动态链接（`libjpeg-8.dll` 依赖）

原因：cmake 的 FindJPEG 模块默认优先使用 `.dll.a` 导入库，即使 `JPEG_LIBRARY` 指向 `.a` 静态库，`JPEG_LIBRARY_RELEASE` 仍可能被覆盖为 `.dll.a`。

解决：同时设置 `-DJPEG_LIBRARY` 和 `-DJPEG_LIBRARY_RELEASE` 指向同一个 `.a` 文件。

### WebP 自动检测导致 DLL 依赖

原因：libheif 的 cmake 脚本会自动探测系统中已安装的 WebP 库。

解决：强制设置 `-DWEBP_LIBRARY=""`、`-DWEBPMUX_LIBRARY=""`、`-DWebP_FOUND=FALSE` 并将所有 WebP 相关 cmake 变量置空。

### 编译产物过大（>30 MB）

- 检查是否误链入了 libx265 的 10bit/12bit 变体（MSYS2 x265 包默认包含 multi-bit）
- 确认 `-DCMAKE_BUILD_TYPE=Release` 已设置
- `-s` 链接标志是否生效（`strip` 命令再次去除符号做双重保险）

---

# 附录

## A. UPX 压缩

所有工具编译后均使用 UPX 进行极限压缩。

```bash
# 安装 UPX
pacman -S --needed mingw-w64-x86_64-upx

# 极限压缩
upx --lzma --best <tool>.exe
```

**各工具压缩效果**：

| 工具 | 未压缩 | UPX 后 | 压缩率 |
|------|--------|--------|--------|
| ffmpeg.exe | ~36 MB | **5.8 MB** | 84% |
| heif-enc.exe | 27 MB | **3.1 MB** | 89% |
| heif-dec.exe | 26 MB | **3.0 MB** | 88% |

> **注意**：部分杀毒软件可能误报 UPX 压缩的可执行文件，可在 Windows Defender 中添加排除项。启动时约 0.1 秒解压延迟，用户无感知。

## B. 部署到项目

所有编译产物统一部署到 `LivePhotoBox/Tools/` 目录。

```bash
# 备份旧版本
cp "/d/Projects/live-photo-box/LivePhotoBox/Tools/ffmpeg.exe" \
   "/d/Projects/live-photo-box/LivePhotoBox/Tools/ffmpeg-backup-$(date +%Y%m%d).exe"

# 部署新版本
cp ffmpeg.exe "/d/Projects/live-photo-box/LivePhotoBox/Tools/ffmpeg.exe"
cp heif-enc.exe "/d/Projects/live-photo-box/LivePhotoBox/Tools/heif-enc.exe"
cp heif-dec.exe "/d/Projects/live-photo-box/LivePhotoBox/Tools/heif-dec.exe"
```

**项目中的文件位置**:
- `LivePhotoBox/Tools/` — 所有外部工具二进制
- `LivePhotoBox.Core/Services/ExternalToolLocator.cs` — 工具路径解析
- `.gitignore` 忽略了 `Tools/` 目录（二进制不提交到 git）

## C. 工具目录总览

当前 `Tools/` 目录下的全部外部工具：

| 文件 | 大小 | 用途 | 编译来源 |
|------|------|------|---------|
| `ffmpeg.exe` | 5.8 MB | 视频编解码 + HEIC 解码（mov demuxer） | FFmpeg n8.0.1 定制编译 |
| `heif-enc.exe` | 3.1 MB | JPEG → HEIC 编码 | libheif v1.23.1 定制编译 |
| `heif-dec.exe` | 3.0 MB | HEIC → JPEG 解码 | libheif v1.23.1 定制编译 |
| `exiftool.exe` | 57 KB | 元数据读写 | 官方 Perl 编译 |
| `jpegtran.exe` | 156 KB | JPEG 无损旋转 | libjpeg-turbo |
| **合计** | **12.1 MB** | | |

> **维护提示**：
> - 项目新增 ffmpeg 参数时，同步更新本文档第 1 节的功能清单
> - 重新编译任一工具后，更新文档头部"最后编译"日期和对应版本号
> - 部署新版本前务必先备份当前 `Tools/` 目录
