# Live Photo Box 开发工具清单

> **创建日期**: 2026-07-23
> **目的**: 建立"你给需求 → Claude 规划执行 → 自动测试 → 自动修复 → 你验收"的自主开发流水线
> **最后更新**: 2026-07-23 22:35

---

## 安装进度总览

| # | 工具 | 状态 | 备注 |
|---|------|:----:|------|
| 1 | Claude Code Hooks | ✅ | `.claude/settings.json` — PostToolUse → auto `dotnet build` |
| 2 | Claude Code Skills | ✅ | `live-photo-box` + `winui3-patterns` 两个 skill |
| 3 | BenchmarkDotNet | ✅ | `tests/LivePhotoBox.Benchmarks/` — 已验证，PathHelper 2 个 benchmark 通过 |
| 4 | Everything | ✅ | CLI `es.exe` 可用，秒搜文件名 |
| 5 | Flawright | ✅ | `tests/LivePhotoBox.UITests/` — 包已恢复，待写测试 |
| 6 | WinApp MCP | ✅ | npm 全局安装，待配置 MCP |
| 7 | OfficeCLI | ⚠️ | **Defender 锁文件**，需临时关实时保护后 `npm install -g @officecli/officecli` |
| 8 | Director Mode Lite | 🔒 | **GitHub 被墙**，需 VPN 后 `git clone` |
| 9 | Finn-loop | 🔒 | **GitHub 被墙**，需 VPN 后 clone + 配置 Linear |
| 10 | coverlet + ReportGenerator | ✅ | coverlet 已加入 StressTest 项目，ReportGenerator 5.5.10 全局安装 |
| 11 | dotMemory | ⬜ | 需从 JetBrains 官网下载安装 |
| 12 | Graphify | ✅ | v0.9.25，支持 25 种语言 tree-sitter 解析 |
| 13 | Sysinternals | ✅ | procexp/procmon/pslist 等全部在 PATH |
| 14 | fd | ✅ | v10.4.2，比 `dir` 快 10× |
| 15 | Prompt Optimizer | 🌐 | 网页版 `prompt.always200.com`，无需安装 |
| 16 | Hallmark | 🔒 | **GitHub 被墙**，需 VPN 后 `npx skills add nutlope/hallmark` |
| 17 | Playwright | ✅ | v1.61.0，已加入 UITests 项目 |

> ✅ 已验证可用 | ⚠️ 需手动处理 | 🔒 需 VPN | ⬜ 待安装 | 🌐 在线用

---

## 统计

| 状态 | 数量 | 工具 |
|:----:|:----:|------|
| ✅ 可用 | **12** | Hooks, Skills, BenchmarkDotNet, Everything, Flawright, WinApp MCP, coverlet, Graphify, Sysinternals, fd, Playwright, ReportGenerator |
| ⚠️ 手动 | **1** | OfficeCLI（关 Defender → 重装） |
| 🔒 需VPN | **3** | Director Mode Lite, Finn-loop, Hallmark |
| ⬜ 待装 | **1** | dotMemory |
| 🌐 在线 | **1** | Prompt Optimizer |

---

## 项目新增文件

| 文件 | 用途 |
|------|------|
| `.claude/settings.json` | Hooks 配置（自动编译） |
| `.claude/skills/live-photo-box/SKILL.md` | 项目全局规则 skill |
| `.claude/skills/winui3-patterns/SKILL.md` | WinUI 3 开发规则 skill |
| `tests/LivePhotoBox.Benchmarks/` | BenchmarkDotNet 性能测试项目 |
| `tests/LivePhotoBox.UITests/` | Flawright + Playwright UI 测试项目 |

---

## 每个工具怎么用（速查）

### Claude Code Hooks
改代码 → 自动 `dotnet build`。不用手动操作。配置在 `.claude/settings.json`。

### Claude Code Skills
- `/live-photo-box`：加载项目全局规则（架构、命名、外部工具、协议、发布流程）
- `/winui3-patterns`：加载 WinUI 3 开发规则（RESW、XAML、ContentDialog、控件规则）

### BenchmarkDotNet
```bash
cd tests/LivePhotoBox.Benchmarks
dotnet run -c Release -- --filter "*"
```
结果在 `BenchmarkDotNet.Artifacts/results/`。加新 benchmark：在 `PathHelperBenchmarks.cs` 加 `[Benchmark]` 方法。

### Everything
```bash
es.exe "关键词"              # CLI 搜索
es.exe -path "目录" "文件名"  # 限定目录搜索
```
GUI：`C:\Program Files\Everything\Everything.exe`

### Flawright
WinUI 3 UI 测试。新建测试继承 Flawright API：
```csharp
await using var fw = await Flawright.LaunchAsync(new LaunchOptions { ... });
var page = await fw.Browser.NewPageAsync();
await page.Locator("#Button").ClickAsync();
```

### WinApp MCP
在 `.claude/mcp.json` 中配置后，Claude 可直接操控 Live Photo Box 窗口。
```json
{
  "mcpServers": {
    "winapp": {
      "command": "winapp-mcp",
      "args": []
    }
  }
}
```

### coverlet + ReportGenerator
```bash
dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura
reportgenerator -reports:coverage.cobertura.xml -targetdir:coverage-report
```

### Graphify
```bash
cd D:/Projects/live-photo-box
graphify .              # 分析当前目录
graphify query "..."    # 查询知识图谱
```
输出在 `graphify-out/`：`graph.html`（交互图）+ `GRAPH_REPORT.md`（审计报告）

### Sysinternals
```bash
procexp          # 进程浏览器 GUI
procmon          # 进程监控 GUI
pslist exiftool  # 查看 exiftool 进程
```

### fd
```bash
fd "关键词"              # 搜索文件名
fd -e cs "Repair"       # 搜索 .cs 文件
fd -e xaml .            # 列出所有 XAML 文件
```

### Playwright
Web 测试（备用）。用 `InstallAsync()` 下载浏览器：
```csharp
await Playwright.CreateAsync().Result.Chromium.LaunchAsync();
```

---

## 需要你手动处理的事项

1. **OfficeCLI**：关掉 Windows Defender 实时保护 → 跑 `npm install -g @officecli/officecli`
2. **Director Mode Lite / Finn-loop / Hallmark**：开 VPN → 我来 clone
3. **dotMemory**：去 [JetBrains 官网](https://www.jetbrains.com/dotmemory/) 下载安装
