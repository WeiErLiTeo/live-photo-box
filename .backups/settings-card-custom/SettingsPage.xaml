<!-- 设置页（新版）：外观、转码、合成、拆分、修复配置及调试工具 -->
<Page
    x:Class="LivePhotoBox.Views.SettingsPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:controls="using:LivePhotoBox.Controls"
    mc:Ignorable="d" Background="Transparent"
    NavigationCacheMode="Required">

    <Page.Resources>
        <!-- 轻量设置卡片样式（供少数不使用 SettingsCard 控件的特殊卡片使用） -->
        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Background" Value="{ThemeResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,12"/>
        </Style>

        <!-- 设置卡片内的 ToggleSwitch：去掉 On/Off 文字，只保留开关滑块。
             描述文字已由 SettingsCard 的 Header / Description 提供，无需重复显示。 -->
        <Style x:Key="SettingsToggleSwitchStyle" TargetType="ToggleSwitch">
            <Setter Property="OnContent" Value=""/>
            <Setter Property="OffContent" Value=""/>
        </Style>
    </Page.Resources>

    <!-- ═══════════════════════════════════════════════════════════════════════
         设置页面硬性规则（维护者必读）：

         1. 多语言
            页面中的所有文本（标题、描述、按钮、链接等）必须通过
            x:Uid 引用 Resources.resw，严禁在 Text / Content 属性中
            硬编码中文或任何语言的文字。

         2. 描述文本末尾不要句号
            所有 SettingsCard.Description 中的详细解释文本，
            末尾一律不加句号（。），保持简洁。
            例：✓ "用于查看 LivePhotoBox 处理过的照片操作记录"
                 ✗ "用于查看 LivePhotoBox 处理过的照片操作记录。"

         3. 调试工具区颜色
            本区域的交互控件（ToggleSwitch、Slider）必须使用红色
            （#D32F2F）覆写，不跟随主题色，参见下方注释。

         4. 统一卡片控件
            所有标准设置卡片使用 <controls:SettingsCard>，传入
            Icon / Header / Description / ActionContent 四个属性。
            不要手动重复 Grid + Border 骨架。
         ═══════════════════════════════════════════════════════════════════════ -->

    <!-- 布局：第0行固定标题，第1行可滚动内容 -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 固定标题 -->
        <TextBlock Grid.Row="0"
                   x:Uid="SettingsPage_Title" FontSize="32" FontWeight="Bold"
                   Margin="40,24,40,8" FontFamily="Microsoft YaHei UI"/>

        <ScrollViewer Grid.Row="1" x:Name="PageScrollViewer" Padding="40,0,40,0">
        <StackPanel x:Name="ContentPanel" Spacing="28" HorizontalAlignment="Stretch" Margin="0,0,0,40">

            <!-- ====================================================================== -->
            <!--  外观与个性化                                                         -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE790;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Appearance_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 语言 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xF2B7;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Language"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Language_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ComboBox SelectedIndex="{x:Bind ViewModel.LanguageIndex, Mode=TwoWay}"
                                  Loaded="AppearanceComboBox_Loaded" MinWidth="200">
                            <ComboBoxItem x:Uid="SettingsPage_Language_System"/>
                            <ComboBoxItem Content="English (en-US)"/>
                            <ComboBoxItem Content="中文(简体) (zh-Hans)"/>
                        </ComboBox>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 主题 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xE790;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Theme_Header"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Theme_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ComboBox SelectedIndex="{x:Bind ViewModel.ElementTheme, Mode=TwoWay}"
                                  Loaded="AppearanceComboBox_Loaded" MinWidth="200">
                            <ComboBoxItem x:Uid="SettingsPage_Theme_System"/>
                            <ComboBoxItem x:Uid="SettingsPage_Theme_Light"/>
                            <ComboBoxItem x:Uid="SettingsPage_Theme_Dark"/>
                        </ComboBox>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 背景材质 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xEB91;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Backdrop_Header"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Backdrop_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ComboBox SelectedIndex="{x:Bind ViewModel.BackdropIndex, Mode=TwoWay}"
                                  Loaded="AppearanceComboBox_Loaded" MinWidth="200">
                            <ComboBoxItem x:Uid="SettingsPage_Backdrop_Mica"/>
                            <ComboBoxItem x:Uid="SettingsPage_Backdrop_MicaAlt"/>
                            <ComboBoxItem x:Uid="SettingsPage_Backdrop_Acrylic"/>
                            <ComboBoxItem x:Uid="SettingsPage_Backdrop_AcrylicLight"/>
                            <ComboBoxItem x:Uid="SettingsPage_Backdrop_None"/>
                        </ComboBox>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- Acrylic 浓度滑块（仅选 Acrylic / Acrylic 薄透 时可见） -->
                <controls:SettingsCard Visibility="{x:Bind ViewModel.BackdropIndex, Mode=OneWay, Converter={StaticResource BackdropToAcrylicVisibilityConverter}}">
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="SquareHintSparkles"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_AcrylicOpacity_Header"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_AcrylicOpacity_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <Slider Minimum="0.0" Maximum="1.0"
                                    Value="{x:Bind ViewModel.AcrylicTintOpacity, Mode=TwoWay}"
                                    StepFrequency="0.05" Width="220" VerticalAlignment="Center"
                                    ThumbToolTipValueConverter="{StaticResource DoubleToPercentConverter}"/>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4"
                                    Width="52" Height="36"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                                           FontSize="13" FontWeight="SemiBold"
                                           Text="{x:Bind ViewModel.AcrylicTintOpacityText, Mode=OneWay}"/>
                            </Border>
                        </StackPanel>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 窗口整体透明度 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xF5EF;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_WindowOpacity_Header"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_WindowOpacity_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <Slider Minimum="0.1" Maximum="1.0"
                                    Value="{x:Bind ViewModel.WindowOpacity, Mode=TwoWay}"
                                    StepFrequency="0.01" Width="220" VerticalAlignment="Center"
                                    ThumbToolTipValueConverter="{StaticResource DoubleToPercentConverter}"/>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4"
                                    Width="52" Height="36"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                                           FontSize="13" FontWeight="SemiBold"
                                           Text="{x:Bind ViewModel.WindowOpacityText, Mode=OneWay}"/>
                            </Border>
                        </StackPanel>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 主页背景（可折叠）— 特殊卡片，保留原始 Border 结构 -->
                <Border Style="{StaticResource SettingsCardStyle}" Padding="0">
                    <StackPanel>
                        <Grid Padding="22,12" ColumnSpacing="22" Tapped="BannerExpanderHeader_Tapped">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0" Glyph="&#xF58A;" FontSize="20"
                                      Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                      VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock x:Uid="SettingsPage_Banner_Header"/>
                                <TextBlock x:Uid="SettingsPage_Banner_Desc" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <FontIcon x:Name="BannerExpanderChevron" Grid.Column="2"
                                      Glyph="&#xE70D;" FontSize="12"
                                      Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <StackPanel x:Name="BannerExpanderContent" Visibility="Visible">
                            <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"
                                    Margin="16,0,16,4"/>

                            <!-- 随机切换开关（嵌套卡片，用内部 Border 样式） -->
                            <Border Style="{StaticResource SettingsCardStyle}" Margin="8,4,8,8"
                                    BorderThickness="0" Padding="16,8">
                                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="22">
                                    <FontIcon Grid.Column="0" Glyph="&#xE7F8;" FontSize="20"
                                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                              VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_BannerRandom_Header"/>
                                        <TextBlock x:Uid="SettingsPage_BannerRandom_Desc" FontSize="12"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                  IsOn="{x:Bind ViewModel.IsBannerRandomEnabled, Mode=TwoWay}"
                                                  x:Uid="Toggle_OnOff"/>
                                </Grid>
                            </Border>

                            <!-- 折叠内容：左预览图 + 右当前预设名称 -->
                            <Border Style="{StaticResource SettingsCardStyle}" Margin="8,0,8,8"
                                    BorderThickness="0" Padding="16,8">
                                <Grid HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" CornerRadius="8" Width="320" Height="135">
                                        <Grid>
                                            <Image Source="{x:Bind ViewModel.BannerImage0, Mode=OneWay}"
                                                   Visibility="{x:Bind ViewModel.Banner0Visible, Mode=OneWay}"
                                                   Stretch="UniformToFill"
                                                   VerticalAlignment="Stretch"
                                                   HorizontalAlignment="Center"/>
                                            <Image Source="{x:Bind ViewModel.BannerImage1, Mode=OneWay}"
                                                   Visibility="{x:Bind ViewModel.Banner1Visible, Mode=OneWay}"
                                                   Stretch="UniformToFill"
                                                   VerticalAlignment="Stretch"
                                                   HorizontalAlignment="Center"/>
                                            <Image Source="{x:Bind ViewModel.BannerImage2, Mode=OneWay}"
                                                   Visibility="{x:Bind ViewModel.Banner2Visible, Mode=OneWay}"
                                                   Stretch="UniformToFill"
                                                   VerticalAlignment="Stretch"
                                                   HorizontalAlignment="Center"/>
                                            <Button HorizontalAlignment="Left" VerticalAlignment="Center"
                                                    Click="PrevBanner_Click"
                                                    Background="#33000000" BorderThickness="0"
                                                    Width="32" Height="48" Padding="0"
                                                    CornerRadius="0,4,4,0">
                                                <FontIcon Glyph="&#xE76B;" FontSize="14" Foreground="White"/>
                                            </Button>
                                            <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                                                    Click="NextBanner_Click"
                                                    Background="#33000000" BorderThickness="0"
                                                    Width="32" Height="48" Padding="0"
                                                    CornerRadius="4,0,0,4">
                                                <FontIcon Glyph="&#xE76C;" FontSize="14" Foreground="White"/>
                                            </Button>
                                        </Grid>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                                        <TextBlock x:Uid="SettingsPage_Banner_CurrentLabel" FontSize="13"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{x:Bind ViewModel.CurrentBannerPresetName, Mode=OneWay}"
                                                   FontSize="18" FontWeight="SemiBold"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  通用                                                                 -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE713;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_General_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 递归扫描 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xE838;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_General_RecursiveScan"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_General_RecursiveScan_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsRecursiveScanEnabled, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 输出保留子目录（条件可见） -->
                <controls:SettingsCard Visibility="{x:Bind ViewModel.OutputPreserveSubfolderVisibility, Mode=OneWay}">
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xE8A4;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_General_OutputPreserveSubfolder"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_General_OutputPreserveSubfolder_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsOutputPreserveSubfolderStructure, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  视频转码                                                             -->
            <!-- ====================================================================== -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xEA69;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Transcode_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 硬件编码器（ComboBox + 刷新按钮） -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xEEA1;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Transcode_Hardware"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Transcode_Hardware_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                            <ComboBox x:Name="HardwareComboBox"
                                      ItemsSource="{x:Bind ViewModel.AvailableHardware, Mode=OneWay}"
                                      SelectedItem="{x:Bind ViewModel.SelectedHardware, Mode=TwoWay}"
                                      DisplayMemberPath="Name"
                                      Loaded="HardwareComboBox_Loaded"
                                      MinWidth="200"/>
                            <Button Width="32" Height="32" Padding="0"
                                    x:Uid="SettingsPage_HardwareRefresh_Button"
                                    Command="{x:Bind ViewModel.RefreshHardwareCommand, Mode=OneWay}">
                                <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                            </Button>
                        </StackPanel>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 线程数 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="FastForward"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Transcode_ThreadCount"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Transcode_ThreadCount_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <Slider Minimum="1"
                                    Maximum="{x:Bind ViewModel.MaxThreadCount, Mode=OneWay}"
                                    Value="{x:Bind ViewModel.ThreadCount, Mode=TwoWay}"
                                    StepFrequency="1" Width="220" VerticalAlignment="Center"/>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4"
                                    MinWidth="36" Height="36"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind ViewModel.ThreadCount, Mode=OneWay}"
                                           TextAlignment="Center" FontSize="15" FontWeight="SemiBold"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Margin="8,0"/>
                            </Border>
                        </StackPanel>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  实况照片合成                                                         -->
            <!-- ====================================================================== -->
            <StackPanel x:Name="MergeSettingsHeader" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE8B1;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Merge_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 照片与视频配对方式 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="FilmstripImage"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_MetadataMatchingLabel"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Merge_MetadataMatchingDesc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ComboBox SelectedIndex="{x:Bind ViewModel.MetadataMatchingModeIndex, Mode=TwoWay}"
                                  Loaded="AppearanceComboBox_Loaded" MinWidth="200">
                            <ComboBoxItem x:Uid="SettingsPage_Merge_MetadataMatching_FilenameAndCid"/>
                            <ComboBoxItem x:Uid="SettingsPage_Merge_MetadataMatching_FilenameCidAndMetadata"/>
                            <ComboBoxItem x:Uid="SettingsPage_Merge_MetadataMatching_Filename"/>
                            <ComboBoxItem x:Uid="SettingsPage_Merge_MetadataMatching_Cid"/>
                            <ComboBoxItem x:Uid="SettingsPage_Merge_MetadataMatching_Metadata"/>
                        </ComboBox>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- HEIC 解码器（带说明链接） -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="DeveloperBoardSearch"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <StackPanel Spacing="2">
                            <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder_Desc" FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            <toolkit:WrapPanel HorizontalSpacing="16" VerticalSpacing="4">
                                <HyperlinkButton FontSize="12" Padding="0" Click="OpenHeifStoreLink_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder_HeifLinkText" VerticalAlignment="Center"/>
                                        <FontIcon Glyph="&#xE8A7;" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </HyperlinkButton>
                                <HyperlinkButton FontSize="12" Padding="0" Click="OpenHevcStoreLink_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock x:Uid="SettingsPage_Merge_HeicDecoder_HevcLinkText" VerticalAlignment="Center"/>
                                        <FontIcon Glyph="&#xE8A7;" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </HyperlinkButton>
                            </toolkit:WrapPanel>
                        </StackPanel>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ComboBox SelectedIndex="{x:Bind ViewModel.HeicDecoderIndex, Mode=TwoWay}"
                                  Loaded="AppearanceComboBox_Loaded" MinWidth="200">
                            <ComboBoxItem x:Uid="SettingsPage_Merge_HeicDecoder_MagickNET"/>
                            <ComboBoxItem x:Uid="SettingsPage_Merge_HeicDecoder_BitmapDecoder"/>
                        </ComboBox>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 合成线程数 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="FastForward"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_ThreadCount"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Merge_ThreadCount_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <Slider Minimum="1"
                                    Maximum="{x:Bind ViewModel.MaxMergeThreadCount, Mode=OneWay}"
                                    Value="{x:Bind ViewModel.MergeThreadCount, Mode=TwoWay}"
                                    StepFrequency="1" Width="220" VerticalAlignment="Center"/>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4"
                                    MinWidth="36" Height="36"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind ViewModel.MergeThreadCount, Mode=OneWay}"
                                           TextAlignment="Center" FontSize="15" FontWeight="SemiBold"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Margin="8,0"/>
                            </Border>
                        </StackPanel>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 强制 MP4 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="VideoSync"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Merge_GoogleForceMp4"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Merge_GoogleForceMp4_Desc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsGoogleProtocolForceMp4, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  实况照片拆分                                                         -->
            <!-- ====================================================================== -->
            <StackPanel x:Name="SplitSettingsHeader" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE8C6;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Split_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 视频输出格式 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="VideoMultiple"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Split_FormatLabel"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Split_FormatDesc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ComboBox SelectedIndex="{x:Bind ViewModel.SplitFormatIndex, Mode=TwoWay}"
                                  Loaded="AppearanceComboBox_Loaded" MinWidth="200">
                            <ComboBoxItem x:Uid="SettingsPage_SplitFormat_ImageDefault"/>
                            <ComboBoxItem x:Uid="SettingsPage_SplitFormat_MP4"/>
                            <ComboBoxItem x:Uid="SettingsPage_SplitFormat_MOV"/>
                        </ComboBox>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  实况照片显示修复                                                     -->
            <!-- ====================================================================== -->
            <StackPanel x:Name="RepairSettingsHeader" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                    <FontIcon Glyph="&#xE7C5;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock x:Uid="SettingsPage_Repair_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                </StackPanel>

                <!-- 仅修复 Apple 设备拍摄的照片 -->
                <controls:SettingsCard x:Name="AppleOnlyScanCard">
                    <controls:SettingsCard.Icon>
                        <FontIcon Glyph="&#xE90F;" FontSize="20"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_AppleOnlyScanLabel"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_AppleOnlyScanDesc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsAppleOnlyScanEnabled, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- HEIC 原图修复 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="ScanCamera" FontSize="16"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Repair_HeicLabel"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Repair_HeicDesc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsHeicRepairEnabled, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 非实况视频修复 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="VideoSettings"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Repair_LivePhotoVideoLabel"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Repair_LivePhotoVideoDesc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsNonLivePhotoVideoRepairEnabled, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>

                <!-- 更严格的实况照片扫描（带高亮叠加层） -->
                <Grid x:Name="StrictLivePhotoScanRoot">
                    <controls:SettingsCard x:Name="StrictLivePhotoScanCard">
                        <controls:SettingsCard.Icon>
                            <ic:SymbolIcon Symbol="FolderSearch"
                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        </controls:SettingsCard.Icon>
                        <controls:SettingsCard.Header>
                            <TextBlock x:Uid="SettingsPage_StrictLivePhotoScanLabel"/>
                        </controls:SettingsCard.Header>
                        <controls:SettingsCard.Description>
                            <TextBlock x:Uid="SettingsPage_StrictLivePhotoScanDesc" FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        </controls:SettingsCard.Description>
                        <controls:SettingsCard.ActionContent>
                            <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsStrictLivePhotoScanEnabled, Mode=TwoWay}"
                                          x:Uid="Toggle_OnOff"/>
                        </controls:SettingsCard.ActionContent>
                    </controls:SettingsCard>
                    <Border x:Name="StrictLivePhotoScanHighlight" IsHitTestVisible="False"
                            Background="Transparent" CornerRadius="4"/>
                </Grid>

                <!-- CopyPerfect 输出 -->
                <controls:SettingsCard>
                    <controls:SettingsCard.Icon>
                        <ic:SymbolIcon Symbol="ImageArrowForward"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </controls:SettingsCard.Icon>
                    <controls:SettingsCard.Header>
                        <TextBlock x:Uid="SettingsPage_Repair_CopyPerfectLabel"/>
                    </controls:SettingsCard.Header>
                    <controls:SettingsCard.Description>
                        <TextBlock x:Uid="SettingsPage_Repair_CopyPerfectDesc" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                    </controls:SettingsCard.Description>
                    <controls:SettingsCard.ActionContent>
                        <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsCopyPerfectToOutput, Mode=TwoWay}"
                                      x:Uid="Toggle_OnOff"/>
                    </controls:SettingsCard.ActionContent>
                </controls:SettingsCard>
            </StackPanel>

            <!-- ====================================================================== -->
            <!--  调试工具区                                                           -->
            <!-- ====================================================================== -->
            <StackPanel Margin="0,0,0,0" Spacing="16">
                <Grid ColumnSpacing="22">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton Grid.Column="0"
                                  IsChecked="{x:Bind IsTestToolsVisible, Mode=TwoWay}"
                                  HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xEC7A;" FontSize="14"/>
                            <TextBlock Text="{x:Bind TestToolsToggleButtonText, Mode=OneWay}" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ToggleButton>

                    <Button Grid.Column="1"
                            Click="RestoreDefaultSettings_Click"
                            HorizontalAlignment="Right"
                            BorderThickness="0"
                            Style="{ThemeResource DefaultButtonStyle}">
                        <Button.Resources>
                            <SolidColorBrush x:Key="ButtonBackground" Color="#D32F2F"/>
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#EF5350"/>
                            <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#C62828"/>
                            <SolidColorBrush x:Key="ButtonForeground" Color="White"/>
                            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White"/>
                            <SolidColorBrush x:Key="ButtonForegroundPressed" Color="White"/>
                            <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent"/>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE72C;" FontSize="14" Foreground="White"/>
                            <TextBlock x:Uid="SettingsPage_Restore_Button_Text" FontSize="13" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <TextBlock Text="{x:Bind CrashNoticeText, Mode=OneTime}"
                           Visibility="{x:Bind CrashNoticeVisibility, Mode=OneWay}"
                           Foreground="{ThemeResource SystemErrorTextColor}"
                           FontSize="12"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,16"/>

                <StackPanel Spacing="20"
                            Visibility="{x:Bind TestToolsVisibility, Mode=OneWay}">

                    <!-- 应用日志 — 特殊布局卡片，保留原始 Border 结构 -->
                    <StackPanel Spacing="4">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                            <FontIcon Glyph="&#xE7BA;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                            <TextBlock x:Uid="SettingsPage_Crash_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                        </StackPanel>

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="22">
                                <ic:SymbolIcon Grid.Column="0" Symbol="ClipboardTextEdit"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               VerticalAlignment="Top" Margin="0,2,0,0"/>
                                <StackPanel Grid.Column="1" Spacing="12">
                                    <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                        <TextBlock x:Uid="SettingsPage_CrashLastTime_Label" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                        <HyperlinkButton Command="{x:Bind AboutViewModel.OpenLatestCrashLogActionCommand, Mode=OneWay}" Padding="0" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind AboutViewModel.LastCrashFileNameText, Mode=OneWay}" TextWrapping="Wrap" FontSize="15"/>
                                        </HyperlinkButton>
                                    </StackPanel>
                                    <toolkit:WrapPanel HorizontalSpacing="8" VerticalSpacing="8">
                                        <Button Command="{x:Bind AboutViewModel.OpenCrashLogFolderActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE838;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashOpenFolder_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind AboutViewModel.ExportLatestCrashLogActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashExportLatest_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind AboutViewModel.ClearCrashLogsActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashClear_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind AboutViewModel.OpenIssueFeedbackActionCommand, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE8A7;" FontSize="14"/>
                                                <TextBlock x:Uid="SettingsPage_CrashIssue_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </toolkit:WrapPanel>

                                    <TextBlock x:Uid="SettingsPage_CrashLogPrivacyHint"
                                               FontSize="11"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- ==================== 调试工具 — 全部统一使用 SettingsCard ==================== -->

                    <!-- ⚠️ 调试工具区通用规范 ⚠️

                         颜色：
                         本区域所有能改变主题色的交互控件（ToggleSwitch、Slider、按钮等），
                         必须统一使用红色（#D32F2F 及其 Hover/Pressed 变体）作为覆写颜色，
                         不得跟随系统主题色（SystemAccentColor）。
                         原因：调试工具属于危险/实验性功能，红色起到"警告"作用，
                         提醒用户这些设置可能会影响程序正常运行。

                         开关文本：
                         本区域的所有 ToggleSwitch 必须使用 x:Uid="Toggle_EnabledDisabled"，
                         使其显示"已启用 / 已禁用"而不是默认的"开 / 关"，
                         语义更符合调试功能的性质。

                         如果新增任何带颜色的控件（开关、滑块、按钮、单选等），
                         请参照下方已有控件的 Resources 写法添加红色覆写。 -->
                    <StackPanel Spacing="4">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,6">
                            <FontIcon Glyph="&#xEBE8;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}"/>
                            <TextBlock x:Uid="SettingsPage_Test_Header" FontSize="18" FontWeight="SemiBold" FontFamily="Microsoft YaHei UI"/>
                        </StackPanel>

                        <TextBlock x:Uid="SettingsPage_Test_Warning"
                                   FontSize="12"
                                   Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12"/>

                        <!-- 操作按钮 — 特殊卡片（WrapPanel 按钮组） -->
                        <Border Style="{StaticResource SettingsCardStyle}">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="22">
                                <FontIcon Grid.Column="0" Glyph="&#xEC7A;" FontSize="20"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          VerticalAlignment="Top" Margin="0,2,0,0"/>
                                <toolkit:WrapPanel Grid.Column="1" HorizontalSpacing="8" VerticalSpacing="8">
                                    <Button Click="CheckUpdate_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE895;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_CheckUpdate_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Click="RestartAppButton_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_RestartApp_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Click="PreviewCrashDialogButton_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE8A1;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_CrashPreviewDialog_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Click="SwitchToClassic_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE8AB;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPageModern_SwitchToClassic_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Click="CheckExternalTools_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE9D2;" FontSize="14"/>
                                            <TextBlock x:Uid="SettingsPage_CheckTools_Button_Text" FontSize="13" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </toolkit:WrapPanel>
                            </Grid>
                        </Border>

                        <!-- GitHub API Token — 特殊卡片（PasswordBox + 多按钮） -->
                        <Border x:Name="GitHubTokenCard" Style="{StaticResource SettingsCardStyle}">
                            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="22">
                                <FontIcon Grid.Column="0" Glyph="&#xE72E;" FontSize="20"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          VerticalAlignment="Top" Margin="0,2,0,0"/>
                                <StackPanel Grid.Column="1" Spacing="2">
                                    <TextBlock x:Uid="SettingsPage_Debug_GitHubTokenHeader"/>
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="SettingsPage_Debug_GitHubTokenDesc" FontSize="12"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                                        <HyperlinkButton FontSize="12" Padding="0" Click="OpenGitHubTokenGuide_Click">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Uid="SettingsPage_Debug_GitHubTokenGuideLink" VerticalAlignment="Center"/>
                                                <FontIcon Glyph="&#xE8A7;" FontSize="11" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </HyperlinkButton>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Vertical" Spacing="8" MinWidth="320" Margin="0,4,0,4">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Grid Width="280" Height="32" VerticalAlignment="Center">
                                            <PasswordBox x:Name="GitHubTokenBox"
                                                         Width="280" Height="32"
                                                         IsPasswordRevealButtonEnabled="False"
                                                         VerticalAlignment="Center"/>
                                            <Button x:Name="ClearTokenBoxBtn"
                                                    Width="28" Height="28"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,2,0"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0"
                                                    Click="ClearTokenBox_Click">
                                                <FontIcon Glyph="&#xE711;" FontSize="10"
                                                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                            </Button>
                                        </Grid>
                                        <Button x:Name="SaveGitHubTokenBtn"
                                                x:Uid="SettingsPage_Debug_GitHubTokenSaveBtn"
                                                Click="SaveGitHubToken_Click"
                                                Height="32" Padding="12,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <Grid>
                                        <TextBlock x:Name="GitHubTokenStatus"
                                                   FontSize="11"
                                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                   TextWrapping="Wrap"/>
                                        <Button x:Name="ClearGitHubTokenBtn"
                                                x:Uid="SettingsPage_Debug_GitHubTokenClearBtn"
                                                Click="ClearGitHubToken_Click"
                                                HorizontalAlignment="Right"
                                                Height="24" Padding="8,0"
                                                FontSize="11"
                                                Visibility="Collapsed"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 照片历史页面显示开关 -->
                        <controls:SettingsCard>
                            <controls:SettingsCard.Icon>
                                <FontIcon Glyph="&#xE81C;" FontSize="20"
                                          Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </controls:SettingsCard.Icon>
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_Debug_PhotoHistoryHeader"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_Debug_PhotoHistoryDesc" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.ActionContent>
                                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                    <Button Click="OpenHistoryPage_Click" MinWidth="0" Padding="8,4"
                                            Visibility="{x:Bind ViewModel.IsHistoryPageVisible, Converter={StaticResource InverseVisibilityConverter}}">
                                        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE8E5;" FontSize="12"/>
                                            <TextBlock x:Uid="SettingsPage_Debug_OpenHistoryBtn" FontSize="12"/>
                                        </StackPanel>
                                    </Button>
                                    <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsHistoryPageVisible, Mode=TwoWay}"
                                                  x:Uid="Toggle_EnabledDisabled">
                                        <ToggleSwitch.Resources>
                                            <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="#D32F2F"/>
                                            <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="#EF5350"/>
                                            <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="#C62828"/>
                                        </ToggleSwitch.Resources>
                                    </ToggleSwitch>
                                </StackPanel>
                            </controls:SettingsCard.ActionContent>
                        </controls:SettingsCard>

                        <!-- 写入照片操作记录（红色警告色） -->
                        <controls:SettingsCard>
                            <controls:SettingsCard.Icon>
                                <ic:SymbolIcon Symbol="DocumentImage"
                                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </controls:SettingsCard.Icon>
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_Debug_WriteHistoryHeader"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_Debug_WriteHistoryDesc" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.ActionContent>
                                <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsDetailedHistoryEnabled, Mode=TwoWay}"
                                              x:Uid="Toggle_EnabledDisabled">
                                    <ToggleSwitch.Resources>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="#D32F2F"/>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="#EF5350"/>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="#C62828"/>
                                    </ToggleSwitch.Resources>
                                </ToggleSwitch>
                            </controls:SettingsCard.ActionContent>
                        </controls:SettingsCard>

                        <!-- 扫描缩略图开关（红色警告色） -->
                        <controls:SettingsCard>
                            <controls:SettingsCard.Icon>
                                <ic:SymbolIcon Symbol="FolderSearch"
                                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </controls:SettingsCard.Icon>
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_ScanThumbnailToggleLabel"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_ScanThumbnailToggleDesc" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.ActionContent>
                                <ToggleSwitch Style="{StaticResource SettingsToggleSwitchStyle}" IsOn="{x:Bind ViewModel.IsRepairScanLoadThumbnail, Mode=TwoWay}"
                                              x:Uid="Toggle_EnabledDisabled">
                                    <ToggleSwitch.Resources>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOn" Color="#D32F2F"/>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOnPointerOver" Color="#EF5350"/>
                                        <SolidColorBrush x:Key="ToggleSwitchFillOnPressed" Color="#C62828"/>
                                    </ToggleSwitch.Resources>
                                </ToggleSwitch>
                            </controls:SettingsCard.ActionContent>
                        </controls:SettingsCard>

                        <!-- HEIC 预览并发数（红色警告色滑块） -->
                        <controls:SettingsCard>
                            <controls:SettingsCard.Icon>
                                <FontIcon Glyph="&#xE7C5;" FontSize="20"
                                          Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </controls:SettingsCard.Icon>
                            <controls:SettingsCard.Header>
                                <TextBlock x:Uid="SettingsPage_HeicConcurrencyLabel"/>
                            </controls:SettingsCard.Header>
                            <controls:SettingsCard.Description>
                                <TextBlock x:Uid="SettingsPage_HeicConcurrencyDesc" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </controls:SettingsCard.Description>
                            <controls:SettingsCard.ActionContent>
                                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                    <Slider Minimum="1"
                                            Maximum="{x:Bind ViewModel.MaxHeicConcurrency, Mode=OneWay}"
                                            Value="{x:Bind ViewModel.HeicConcurrency, Mode=TwoWay}"
                                            StepFrequency="1" Width="220" VerticalAlignment="Center">
                                        <Slider.Resources>
                                            <SolidColorBrush x:Key="SliderTrackValueFill" Color="#D32F2F"/>
                                            <SolidColorBrush x:Key="SliderTrackValueFillPointerOver" Color="#EF5350"/>
                                            <SolidColorBrush x:Key="SliderTrackValueFillPressed" Color="#C62828"/>
                                            <SolidColorBrush x:Key="SliderThumbBackground" Color="#D32F2F"/>
                                            <SolidColorBrush x:Key="SliderThumbBackgroundPointerOver" Color="#EF5350"/>
                                            <SolidColorBrush x:Key="SliderThumbBackgroundPressed" Color="#C62828"/>
                                        </Slider.Resources>
                                    </Slider>
                                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                            BorderThickness="1" CornerRadius="4"
                                            MinWidth="36" Height="36"
                                            HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind ViewModel.HeicConcurrency, Mode=OneWay}"
                                                   TextAlignment="Center" FontSize="15" FontWeight="SemiBold"
                                                   VerticalAlignment="Center" HorizontalAlignment="Center"
                                                   Margin="8,0"/>
                                    </Border>
                                </StackPanel>
                            </controls:SettingsCard.ActionContent>
                        </controls:SettingsCard>

                    </StackPanel>
                </StackPanel>

                <TextBlock x:Uid="SettingsPage_Footer"
                           FontSize="12" Opacity="0.3"
                           HorizontalAlignment="Center"
                           Margin="0,38,0,20"/>
            </StackPanel>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
